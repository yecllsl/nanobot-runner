# Nanobot Runner 架构图表集

> **文档版本**: v6.0  
> **更新日期**: 2026-04-26  
> **对应架构**: 架构设计说明书_v0.13-0.15.md

---

## 1. 系统整体架构图

```mermaid
graph TB
    subgraph "用户交互层"
        CLI[CLI 命令]
        AGENT_CHAT[Agent Chat<br/>自然语言交互]
        FEISHU_APP[飞书 App]
    end
    
    subgraph "Gateway 接入层"
        GATEWAY[Gateway 服务<br/>飞书通道]
        CHANNEL_MGR[ChannelManager]
        CMD_ROUTER[CommandRouter]
    end
    
    subgraph "Agent 处理层"
        AGENT_LOOP[AgentLoop]
        RUNNER_TOOLS[RunnerTools]
    end
    
    subgraph "业务逻辑层"
        SKILL_MGR[技能管理器]
        TOOL_MGR[工具管理器]
        MEMORY_MGR[记忆管理器]
        PERSONALITY_MGR[人格管理器]
        OBSERVABILITY[可观测性管理器]
        INSIGHT[洞察引擎]
    end
    
    subgraph "nanobot-ai 底座层"
        SKILL_LOADER[SkillsLoader]
        MCP_WRAPPER[MCPToolWrapper]
        MEMORY_STORE[MemoryStore]
        DREAM[Dream]
        AGENT_HOOK[AgentHook]
        MESSAGE_BUS[MessageBus]
    end
    
    subgraph "外部服务层"
        WEATHER[天气服务]
        MAP[地图服务]
        HEALTH[健康数据]
        KNOWLEDGE[知识库]
        FEISHU_API[飞书开放平台 API]
    end
    
    CLI --> SKILL_MGR
    CLI --> TOOL_MGR
    
    AGENT_CHAT -->|"直接对话"| AGENT_LOOP
    AGENT_LOOP --> RUNNER_TOOLS
    
    FEISHU_APP -->|"消息推送"| GATEWAY
    GATEWAY --> CHANNEL_MGR
    CHANNEL_MGR -->|"InboundMessage"| CMD_ROUTER
    CMD_ROUTER -->|"命令匹配"| AGENT_LOOP
    CMD_ROUTER -->|"自然语言"| AGENT_LOOP
    AGENT_LOOP -->|"OutboundMessage"| MESSAGE_BUS
    MESSAGE_BUS -->|"消息响应"| CHANNEL_MGR
    CHANNEL_MGR -->|"HTTP 回调"| FEISHU_API
    FEISHU_API -->|"推送消息"| FEISHU_APP
    
    AGENT_LOOP --> SKILL_LOADER
    RUNNER_TOOLS --> MEMORY_STORE
    MEMORY_MGR --> MEMORY_STORE
    MEMORY_MGR --> DREAM
    PERSONALITY_MGR --> MEMORY_STORE
    OBSERVABILITY --> AGENT_HOOK
    INSIGHT --> MEMORY_STORE
    TOOL_MGR --> MCP_WRAPPER
    
    MCP_WRAPPER --> WEATHER
    MCP_WRAPPER --> MAP
    MCP_WRAPPER --> HEALTH
    SKILL_LOADER --> KNOWLEDGE
```

**架构图说明**：

| 组件 | 职责 | 技术实现 |
|------|------|---------|
| **CLI 命令** | 命令行交互入口，技能管理、工具配置 | `nanobotrun skill`、`nanobotrun tool` |
| **Agent Chat** | 自然语言交互模式，AI 教练对话 | `nanobotrun agent chat` |
| **Gateway 服务** | 飞书通道入口，接收用户消息，返回 AI 响应 | `nanobotrun gateway start` |
| **ChannelManager** | 多通道管理，支持飞书、钉钉等 | `nanobot.channels.manager` |
| **CommandRouter** | 命令路由，区分命令和自然语言 | `nanobot.command.router` |
| **AgentLoop** | AI Agent 主循环，处理消息和工具调用 | `nanobot.agent.AgentLoop` |
| **RunnerTools** | 跑步业务工具集，提供训练数据查询 | `src.agents.tools` |
| **MessageBus** | 消息总线，管理消息流转 | `nanobot.bus.MessageBus` |

---

## 2. 三种用户交互方式对比图

```mermaid
graph LR
    subgraph "用户交互入口"
        CLI[CLI 命令]
        AGENT_CHAT[Agent Chat]
        FEISHU[飞书 App]
    end
    
    subgraph "处理路径"
        CLI_PATH[直接执行<br/>技能/工具管理]
        AGENT_PATH[AgentLoop<br/>自然语言处理]
        GATEWAY_PATH[Gateway<br/>消息路由]
    end
    
    subgraph "核心处理"
        AGENT_LOOP[AgentLoop<br/>AI Agent 主循环]
    end
    
    CLI --> CLI_PATH
    AGENT_CHAT --> AGENT_PATH
    FEISHU --> GATEWAY_PATH
    
    AGENT_PATH --> AGENT_LOOP
    GATEWAY_PATH --> AGENT_LOOP
```

**三种交互方式对比**：

| 对比维度 | CLI 命令 | Agent Chat | 飞书 App |
|---------|---------|-----------|---------|
| **交互方式** | 命令行参数 | 自然语言对话 | 自然语言对话 |
| **使用场景** | 配置管理、数据操作 | 本地开发调试 | 移动办公、日常使用 |
| **响应速度** | 快（直接执行） | 中（LLM 处理） | 中（网络 + LLM） |
| **功能范围** | 全部管理功能 | AI 对话功能 | AI 对话 + 推送通知 |
| **依赖条件** | 本地安装 | 本地安装 + LLM 配置 | Gateway 服务 + 飞书配置 |
| **启动命令** | `nanobotrun <cmd>` | `nanobotrun agent chat` | `nanobotrun gateway start` |

---

## 3. Gateway 消息流数据流图

```mermaid
sequenceDiagram
    participant User as 用户
    participant FeishuApp as 飞书 App
    participant Gateway as Gateway 服务
    participant ChannelMgr as ChannelManager
    participant CmdRouter as CommandRouter
    participant AgentLoop as AgentLoop
    participant Tools as RunnerTools
    participant FeishuAPI as 飞书开放平台 API
    
    User->>FeishuApp: 发送消息 "/stats"
    FeishuApp->>Gateway: 消息推送
    Gateway->>ChannelMgr: 接收 InboundMessage
    ChannelMgr->>CmdRouter: 路由消息
    
    alt 命令匹配
        CmdRouter->>Tools: 执行命令
        Tools-->>CmdRouter: 返回结果
        CmdRouter->>ChannelMgr: 创建 OutboundMessage
    else 自然语言
        CmdRouter->>AgentLoop: 交给 LLM 处理
        AgentLoop->>AgentLoop: 推理决策
        AgentLoop->>Tools: 调用工具
        Tools-->>AgentLoop: 返回数据
        AgentLoop->>ChannelMgr: 创建 OutboundMessage
    end
    
    ChannelMgr->>FeishuAPI: HTTP 回调发送消息
    FeishuAPI->>FeishuApp: 推送消息
    FeishuApp-->>User: 显示响应
```

**消息流说明**：

| 步骤 | 说明 | 数据类型 |
|------|------|---------|
| 1 | 用户通过飞书 App 发送消息 | 文本消息 |
| 2 | Gateway 服务接收消息推送 | HTTP POST |
| 3 | ChannelManager 解析为 InboundMessage | `InboundMessage` |
| 4 | CommandRouter 路由消息 | 命令匹配或自然语言 |
| 5 | 执行命令或 LLM 处理 | 业务逻辑 |
| 6 | 创建 OutboundMessage | `OutboundMessage` |
| 7 | 通过飞书 API 发送响应 | HTTP POST |
| 8 | 飞书 App 显示响应 | 文本消息 |

---

## 4. 技能调用数据流图

```mermaid
sequenceDiagram
    participant User as 用户
    participant Agent as AI Agent
    participant SkillMgr as 技能管理器
    participant SkillLoader as SkillsLoader
    participant Skill as 技能内容
    
    User->>Agent: 询问训练问题
    Agent->>SkillMgr: 查询可用技能
    SkillMgr->>SkillLoader: list_skills()
    SkillLoader-->>SkillMgr: 技能列表
    SkillMgr-->>Agent: 可用技能信息
    
    Agent->>SkillMgr: 加载技能内容
    SkillMgr->>SkillLoader: load_skill()
    SkillLoader->>Skill: 读取 SKILL.md
    Skill-->>SkillLoader: 技能内容
    SkillLoader-->>SkillMgr: 技能内容
    SkillMgr-->>Agent: 技能内容
    
    Agent->>Agent: 结合技能内容生成回复
    Agent-->>User: 返回训练建议
```

---

## 5. 工具调用数据流图

```mermaid
sequenceDiagram
    participant User as 用户
    participant Agent as AI Agent
    participant ToolMgr as 工具管理器
    participant MCP as MCPToolWrapper
    participant External as 外部服务
    
    User->>Agent: 询问明天天气
    Agent->>ToolMgr: 调用天气工具
    ToolMgr->>MCP: call_tool("weather", args)
    MCP->>External: HTTP 请求
    External-->>MCP: 天气数据
    MCP-->>ToolMgr: 工具结果
    ToolMgr-->>Agent: 天气数据
    
    Agent->>Agent: 结合天气数据生成建议
    Agent-->>User: 返回天气+训练建议
```

---

## 6. 记忆管理数据流图

```mermaid
sequenceDiagram
    participant User as 用户
    participant Agent as AI Agent
    participant MemoryMgr as 记忆管理器
    participant MemoryStore as MemoryStore
    participant Dream as Dream
    
    User->>Agent: 多次对话
    Agent->>MemoryMgr: 记录用户偏好
    MemoryMgr->>MemoryStore: write_user()
    MemoryStore-->>MemoryMgr: 写入成功
    
    Note over Dream: 定时运行记忆整理
    Dream->>MemoryStore: read_unprocessed_history()
    MemoryStore-->>Dream: 未处理历史
    Dream->>Dream: 分析并整理记忆
    Dream->>MemoryStore: write_memory()
    
    User->>Agent: 新对话
    Agent->>MemoryMgr: 读取用户偏好
    MemoryMgr->>MemoryStore: read_user()
    MemoryStore-->>MemoryMgr: 用户偏好
    MemoryMgr-->>Agent: 用户偏好
    
    Agent->>Agent: 结合偏好生成个性化回复
    Agent-->>User: 返回个性化建议
```

---

## 7. 可观测性数据流图

```mermaid
sequenceDiagram
    participant User as 用户
    participant Agent as AI Agent
    participant Hook as ObservabilityHook
    participant ObsMgr as 可观测性管理器
    
    User->>Agent: 发送消息
    Agent->>Hook: before_iteration()
    Hook->>ObsMgr: 记录迭代开始
    
    Agent->>Agent: 推理决策
    Agent->>Hook: before_execute_tools()
    Hook->>ObsMgr: 记录工具调用
    
    Agent->>Agent: 执行工具
    Agent->>Hook: after_iteration()
    Hook->>ObsMgr: 记录迭代结束
    
    Agent-->>User: 返回回复
    
    User->>ObsMgr: 查看决策过程
    ObsMgr-->>User: 展示决策追踪
```

---

## 8. 本地部署架构图

```mermaid
graph TB
    subgraph "用户设备"
        CLI[CLI 应用]
        FEISHU_APP[飞书 App]
        DATA[本地数据]
    end
    
    subgraph "Gateway 服务"
        GATEWAY[Gateway 服务<br/>端口 18790]
        CHANNEL_MGR[ChannelManager]
        AGENT_LOOP[AgentLoop]
    end
    
    subgraph "nanobot-ai 底座"
        MEMORY[MemoryStore]
        DREAM[Dream 服务]
        MESSAGE_BUS[MessageBus]
    end
    
    subgraph "外部服务"
        WEATHER[天气 API]
        MAP[地图 API]
        HEALTH[健康数据源]
        FEISHU_API[飞书开放平台 API]
    end
    
    CLI --> GATEWAY
    FEISHU_APP -->|"消息推送"| GATEWAY
    GATEWAY --> CHANNEL_MGR
    CHANNEL_MGR --> AGENT_LOOP
    AGENT_LOOP --> MEMORY
    AGENT_LOOP --> DREAM
    AGENT_LOOP --> MESSAGE_BUS
    AGENT_LOOP --> WEATHER
    AGENT_LOOP --> MAP
    AGENT_LOOP --> HEALTH
    MESSAGE_BUS -->|"HTTP 回调"| FEISHU_API
    FEISHU_API -->|"推送消息"| FEISHU_APP
    MEMORY --> DATA
```

---

## 9. 模块依赖关系图

```mermaid
graph LR
    subgraph "v0.13.0"
        SKILL[技能管理器]
        TOOL[工具管理器]
    end
    
    subgraph "v0.14.0"
        MEMORY[记忆管理器]
        PERSONALITY[人格管理器]
    end
    
    subgraph "v0.15.0"
        OBSERVABILITY[可观测性管理器]
        INSIGHT[洞察引擎]
    end
    
    subgraph "底座依赖"
        NANOBOT[nanobot-ai]
    end
    
    SKILL --> NANOBOT
    TOOL --> NANOBOT
    MEMORY --> NANOBOT
    PERSONALITY --> MEMORY
    OBSERVABILITY --> NANOBOT
    INSIGHT --> MEMORY
```

---

## 10. 数据存储结构图

```mermaid
graph TB
    subgraph "workspace/"
        CONFIG[config.json]
        SOUL[SOUL.md]
        USER[USER.md]
        
        subgraph "memory/"
            MEMORY_MD[MEMORY.md]
            HISTORY[history.jsonl]
            CURSOR[.cursor]
            DREAM_CURSOR[.dream_cursor]
        end
        
        subgraph "skills/"
            WEATHER_SKILL[weather-assistant/]
            TRAINING_SKILL[training-knowledge/]
            CUSTOM_SKILL[custom-skill/]
        end
        
        subgraph "data/"
            ACTIVITIES[activities/]
            PLANS[plans/]
            INSIGHTS[insights/]
        end
    end
```

---

## 11. 版本迭代路线图

```mermaid
gantt
    title Nanobot Runner v0.13-0.15 迭代路线图
    dateFormat  YYYY-MM-DD
    section v0.13.0
    智能技能库           :a1, 2026-05-01, 14d
    外部工具接入         :a2, 2026-05-08, 14d
    section v0.14.0
    AI教练长期记忆       :b1, 2026-05-15, 14d
    AI人格进化           :b2, 2026-05-22, 14d
    section v0.15.0
    全链路可观测性       :c1, 2026-06-01, 14d
    智能洞察引擎         :c2, 2026-06-08, 14d
```

---

## 12. 技术栈架构图

```mermaid
graph TB
    subgraph "应用层"
        CLI_APP[CLI 应用]
        AGENT_APP[Agent 应用]
    end
    
    subgraph "框架层"
        NANOBOT_AI[nanobot-ai 0.1.5.post2]
        TYPER[Typer CLI]
        PYDANTIC[Pydantic-Settings]
    end
    
    subgraph "数据层"
        POLARS[Polars]
        PYARROW[PyArrow]
        FITPARSE[fitparse]
    end
    
    subgraph "基础设施层"
        PYTHON[Python 3.11+]
        UV[uv 包管理]
        PARQUET[Parquet 存储]
    end
    
    CLI_APP --> TYPER
    CLI_APP --> NANOBOT_AI
    AGENT_APP --> NANOBOT_AI
    NANOBOT_AI --> PYDANTIC
    NANOBOT_AI --> POLARS
    POLARS --> PYARROW
    PYARROW --> PARQUET
    FITPARSE --> PARQUET
    TYPER --> PYTHON
    NANOBOT_AI --> PYTHON
    UV --> PYTHON
```

---

## 13. 接口关系图

```mermaid
graph TB
    subgraph "技能管理接口"
        LIST_SKILLS[GET /api/skills]
        ENABLE_SKILL[POST /api/skills/name/enable]
        DISABLE_SKILL[POST /api/skills/name/disable]
        IMPORT_SKILL[POST /api/skills/import]
    end
    
    subgraph "工具管理接口"
        LIST_TOOLS[GET /api/tools]
        CALL_TOOL[POST /api/tools/name/call]
        TOOL_STATUS[GET /api/tools/name/status]
    end
    
    subgraph "记忆管理接口"
        READ_MEMORY[GET /api/memory]
        WRITE_MEMORY[PUT /api/memory]
        READ_USER[GET /api/memory/user]
        WRITE_USER[PUT /api/memory/user]
        MEMORY_VERSIONS[GET /api/memory/versions]
        RESTORE_VERSION[POST /api/memory/versions/version/restore]
    end
    
    subgraph "可观测性接口"
        DECISION_TRACE[GET /api/observability/decisions/session]
        TOOL_TRACE[GET /api/observability/tools/session]
        MEMORY_TRACE[GET /api/observability/memory/session]
    end
```

---

## 14. 风险矩阵图

```mermaid
quadrantChart
    title 风险评估矩阵
    x-axis 低影响 --> 高影响
    y-axis 低概率 --> 高概率
    quadrant-1 重点监控
    quadrant-2 优先处理
    quadrant-3 持续观察
    quadrant-4 接受风险
    R001: [0.35, 0.25]
    R002: [0.45, 0.55]
    R003: [0.75, 0.55]
    R004: [0.85, 0.25]
    R005: [0.45, 0.55]
    R006: [0.75, 0.55]
    R007: [0.25, 0.55]
```

---

*本图表集由架构师基于架构设计说明书自动生成，所有图表均可在 Trae IDE 内直接渲染*

**版本历史**：
- v1.0 (2026-04-17): 初始版本
- v2.0 (2026-04-22): 新增智能跑步计划架构图
- v3.0 (2026-04-26): 全面对齐产品规划 v3.0
- v4.0 (2026-04-26): 基于 nanobot-ai 0.1.5.post2 底座能力全面修订版
- v5.0 (2026-04-26): 新增 Gateway（飞书通道）组件架构图
- v6.0 (2026-04-26): 新增三种用户交互方式对比图，完整体现 CLI、Agent Chat、飞书 App 三种交互入口
