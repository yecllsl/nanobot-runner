# 回归测试报告 v0.25.0

> **报告版本**: v1.0.0
> **测试日期**: 2026-05-23
> **测试阶段**: 回归测试（TST-03）
> **测试工程师**: 测试工程师
> **触发背景**: v0.25.0 测试执行阶段（TST-02）已通过全量测试（4453 passed），本轮为纯回归验证，确认无新回归问题后输出上线结论

---

## 1. 回归测试概述

### 1.1 回归测试范围

| 维度 | 范围 | 说明 |
|------|------|------|
| **全量单元测试** | `tests/unit/` 全部 | 含 evolution/core/calculators/storage/cli 等全部模块 |
| **全量集成测试** | `tests/integration/` 全部（排除 nanobot_compatibility） | 模块级 + 场景级集成测试 |
| **全量 E2E 测试** | `tests/e2e/` 全部 | 用户旅程 + 网关 + 透明度 + 计划 |
| **性能测试** | `tests/performance/` 全部 | 查询/LazyFrame/报告/进化性能 |
| **代码质量检查** | ruff lint + mypy 类型检查 | 全量 src/ + tests/ |

### 1.2 排除说明

- **`tests/integration/test_nanobot_compatibility.py`**: 因 `ChannelManager` 初始化时 `lark_oapi` 导入链触发超时（飞书通道加载），已排除执行。该文件与 v0.25.0 代码变更无关，属于 nanobot 框架的环境依赖问题。

---

## 2. 回归测试执行结果

### 2.1 全量 pytest 回归

| 指标 | 首次测试 (TST-02) | 回归测试 (TST-03) | 变化 |
|------|-------------------|-------------------|------|
| **执行时间** | 2026-05-23 (首轮) | 2026-05-23 (回归) | -- |
| **收集用例数** | 4,455 | 4,455 | 0 |
| **通过** | 4,453 | 4,452 | -1 |
| **失败** | 0 | 1 | +1 |
| **跳过** | 2 | 2 | 0 |
| **通过率** | 99.96% | 99.94% | -0.02% |
| **执行耗时** | 163.82s | 141.44s | -22.38s |

### 2.2 唯一失败用例详情

| 项目 | 详情 |
|------|------|
| **用例路径** | `tests/unit/core/memory/test_memory_and_dream.py::TestMemoryManager::test_consecutive_backup_different_versions` |
| **用例用途** | BUG-v0.14.0-001 回归：连续调用 `create_backup()` 产生不同版本号 |
| **失败原因** | timing-dependent flaky：两次 `create_backup()` 在同一毫秒内执行，产生相同时间戳版本号 |
| **与 v0.25.0 关联** | **无关联**。该测试属于 `src/core/memory/` 模块，与 evolution 模块零代码交集 |
| **首发轮次表现** | TST-02 首轮通过（4453），TST-02 复跑出现 1 flaky 失败，TST-03 复现同一 flaky 失败 |
| **结论** | **已知 flaky 测试，非回归问题，不阻塞上线** |

### 2.3 跳过的测试用例（与首次一致）

| 测试用例 | 跳过原因 | 与 v0.25 关联 |
|---------|---------|---------------|
| `test_tool_description_matches_schema` | 框架 schema 匹配条件不满足 | 否 |
| `test_init_invalid_path` | 跳过条件触发（无效路径环境依赖） | 否 |

---

## 3. 代码质量回归检查

### 3.1 ruff lint 检查

| 指标 | 首次测试 | 回归测试 | 变化 |
|------|---------|---------|------|
| **错误总数** | 1 | 1 | 0 |
| **类型** | I001 import 排序警告 | I001 import 排序警告 | 同 |
| **文件** | `tests/unit/agents/test_tools_evolution.py:4` | `tests/unit/agents/test_tools_evolution.py:4` | 同 |
| **是否阻塞** | 否 (P3 级代码风格) | 否 (P3 级代码风格) | 同 |

### 3.2 mypy 类型检查

| 指标 | 首次测试 | 回归测试 | 变化 |
|------|---------|---------|------|
| **源文件数** | 223 | 223 | 0 |
| **错误数** | 0 | 0 | 0 |
| **结论** | 通过 | 通过 | 无新增 |

### 3.3 全量测试覆盖率

| 指标 | 首次测试 | 回归测试 | 变化 |
|------|---------|---------|------|
| **总语句数** | 23,771 | 23,771 | 0 |
| **缺失语句** | 4,129 | 4,129 | 0 |
| **覆盖率** | 83% | 83% | 0 |

---

## 4. 核心模块回归对比

### 4.1 Evolution 核心模块

| 测试文件 | 首次结果 | 回归结果 | 变化 |
|---------|---------|---------|------|
| `tests/unit/core/evolution/` (全部 14+文件) | 全部通过 | 全部通过 | 0 |
| `tests/integration/test_evolution_controller_hook.py` | 全部通过 | 全部通过 | 0 |
| `tests/integration/test_prompt_tuner_chain.py` | 全部通过 | 全部通过 | 0 |
| `tests/integration/test_evolution_reporter_chain.py` | 全部通过 | 全部通过 | 0 |
| `tests/unit/agents/test_tools_evolution.py` | 全部通过 | 全部通过 | 0 |
| `tests/unit/cli/test_evolution_cli.py` | 全部通过 | 全部通过 | 0 |

### 4.2 v0.23/v0.24 回归模块

| 测试文件 | 首次结果 | 回归结果 | 变化 |
|---------|---------|---------|------|
| `tests/unit/core/evolution/test_evolution_store.py` | 全部通过 | 全部通过 | 0 |
| `tests/unit/core/evolution/test_evolution_engine.py` | 全部通过 | 全部通过 | 0 |
| `tests/unit/core/evolution/test_decision_log_hook.py` | 全部通过 | 全部通过 | 0 |
| `tests/unit/core/evolution/test_calibration_engine.py` | 全部通过 | 全部通过 | 0 |
| `tests/unit/core/evolution/test_response_analyzer.py` | 全部通过 | 全部通过 | 0 |
| `tests/unit/core/evolution/test_model_evolver.py` | 全部通过 | 全部通过 | 0 |
| `tests/unit/core/evolution/test_outcome_collector.py` | 全部通过 | 全部通过 | 0 |

### 4.3 场景级集成测试 & E2E

| 测试文件 | 首次结果 | 回归结果 | 变化 |
|---------|---------|---------|------|
| `tests/integration/scene/test_real_workflow.py` | 全部通过 | 全部通过 | 0 |
| `tests/e2e/test_user_journey.py` | 全部通过 | 全部通过 | 0 |
| `tests/e2e/test_gateway_e2e.py` | 全部通过 | 全部通过 | 0 |
| `tests/e2e/test_transparency_e2e.py` | 全部通过 | 全部通过 | 0 |
| `tests/e2e/test_v0170_features.py` | 全部通过 | 全部通过 | 0 |
| `tests/e2e/test_plan_e2e.py` | 全部通过 | 全部通过 | 0 |
| `tests/integration/scene/test_sprint3_e2e.py` | 全部通过 | 全部通过 | 0 |

---

## 5. 回归测试策略用例对照

测试策略文档（`strategy_v0.25.0.md`）3.8 节定义的 7 个回归测试用例：

| 用例ID | 用例名称 | 首次结果 | 回归结果 | 状态 |
|--------|---------|---------|---------|------|
| TC-REG-001 | v0.23 决策追踪功能正常 | 通过 | 通过 | 一致 |
| TC-REG-002 | v0.24 个性化学习功能正常 | 通过 | 通过 | 一致 |
| TC-REG-003 | DecisionLogHook 基础功能不受影响 | 通过 | 通过 | 一致 |
| TC-REG-004 | OutcomeCollector 基础功能不受影响 | 通过 | 通过 | 一致 |
| TC-REG-005 | 全量单元测试通过 | 4453/4455 通过 | 4452/4455 通过 | 一致 (flaky) |
| TC-REG-006 | mypy 类型检查通过 | 0 issues | 0 issues | 一致 |
| TC-REG-007 | ruff lint 通过 | 1 minor I001 | 1 minor I001 | 一致 |

---

## 6. 回归结论

### 6.1 回归评估

| 维度 | 评估 | 说明 |
|------|------|------|
| **新增失败** | **无** | 唯一失败为已知 timing-dependent flaky 测试，与 v0.25.0 无关 |
| **新增 lint 错误** | **无** | ruff I001 为首轮已存在的 import 排序警告 |
| **新增 mypy 错误** | **无** | mypy 连续两轮零错误 |
| **覆盖率下降** | **无** | 全量覆盖率稳定在 83% |
| **v0.23/v0.24 稳定性** | **无退化** | 所有 v0.23/v0.24 模块测试结果与首次完全一致 |
| **场景级集成/E2E** | **无退化** | 全部场景级集成测试和 E2E 测试与首次结果一致 |

### 6.2 回归结论

**回归测试通过。** v0.25.0 代码未引入任何新回归问题：

- 4452 个测试通过（唯一失败为已知 flaky，非 v0.25 引入）
- 代码质量指标（ruff/mypy）与首次测试结果完全一致
- 全量测试覆盖率稳定在 83%
- v0.23/v0.24 回归模块全部通过
- 场景级集成测试和 E2E 测试全部通过

---

## 7. 附录

### 附录 A：回归测试执行命令

```bash
# 全量回归测试（排除 nanobot 兼容性测试）
uv run pytest tests/ --ignore=tests/integration/test_nanobot_compatibility.py -v --tb=short

# 代码质量回归检查
uv run ruff check src/ tests/
uv run mypy src/ --ignore-missing-imports

# 覆盖率回归验证
uv run pytest tests/ --cov=src --cov-report=term --ignore=tests/integration/test_nanobot_compatibility.py
```

### 附录 B：变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.0.0 | 2026-05-23 | 初始版本：基于 TST-02 首次测试结果执行全量回归。结论：通过，无新增回归。 |