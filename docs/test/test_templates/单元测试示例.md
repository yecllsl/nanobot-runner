# 单元测试示例

```python
import pytest
from pathlib import Path
from unittest.mock import MagicMock, patch

class TestFitParser:
    """FIT文件解析器单元测试"""

    def test_parse_valid_file(self):
        """测试正常FIT文件解析"""
        # Arrange
        parser = FitParser()
        fit_path = Path("tests/data/fixtures/easy_run_20240101.fit")

        # Act
        result = parser.parse_file(fit_path)

        # Assert
        assert result is not None
        assert "activity_id" in result
        assert result["total_distance"] > 0

class TestGoalPredictionEngine:
    """目标达成评估引擎单元测试"""

    def test_predict_marathon_completion(self):
        """测试马拉松目标达成预测"""
        # Arrange
        engine = GoalPredictionEngine()
        user_profile = UserProfile(target_marathon_time="3:30:00")
        training_history = [...]  # 训练历史数据

        # Act
        prediction = engine.predict(user_profile, training_history)

        # Assert
        assert prediction.probability >= 0
        assert prediction.probability <= 1
        assert prediction.confidence in ["high", "medium", "low"]
```
