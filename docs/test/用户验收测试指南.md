# Nanobot Runner 用户验收测试（UAT）指南

> **文档版本**: v8.0（精简版）
> **适用版本**: v0.13.0+
> **更新日期**: 2026-04-28
> **完整用例详情**: [docs/test/uat_test_cases/](docs/test/uat_test_cases/)

---

## 1. 测试概述

### 1.1 测试目的

验证 Nanobot Runner 是否满足业务需求和用户使用预期，确保：
- 核心业务流程完整闭环
- 所有 P0/P1 级功能正常运行
- 数据计算准确（VDOT/TSS/心率漂移等）
- MCP 工具管理和 Agent 工具生态正常运行

### 1.2 测试范围

| 模块 | 覆盖功能 | 优先级 | 用例编号 |
|------|---------|--------|---------|
| 数据导入 | 单文件/批量导入、去重、异常处理 | P0 | UAT-001 ~ UAT-005 |
| 数据查询 | 统计信息、年份/日期范围过滤 | P0 | UAT-006 ~ UAT-008 |
| 数据分析 | VDOT趋势、训练负荷、心率漂移 | P0 | UAT-009 ~ UAT-014 |
| 训练计划 | 智能建议、目标评估、长期规划 | P0 | UAT-015 ~ UAT-017 |
| 报告生成 | 周报、月报、用户画像 | P0 | UAT-018 ~ UAT-020 |
| 系统管理 | 版本、配置验证、初始化迁移 | P1 | UAT-021 ~ UAT-022 |
| Agent交互 | 自然语言对话、数据查询 | P1 | UAT-023 ~ UAT-024 |
| 性能测试 | 批量导入性能、查询性能 | P1 | UAT-025 ~ UAT-027 |
| MCP工具管理 | 工具列表、启用/禁用、配置导入 | P0 | UAT-028 ~ UAT-034 |
| Agent工具集成 | 天气/地图/健康数据工具 | P1 | UAT-035 ~ UAT-037 |
| Gateway服务 | 飞书通道、命令路由、消息推送 | P0 | UAT-038 ~ UAT-042 |

**用例总数**: 42 个（P0: 25个, P1: 17个）

### 1.3 测试排除范围

- 单元测试、集成测试（由开发工程师负责）
- CI/CD 流水线验证（由运维工程师负责）
- 性能压测、安全渗透测试（专项测试）
- LLM 模型输出内容的不确定性验证

---

## 2. 环境配置

### 2.1 环境隔离

| 隔离维度 | 方式 | 说明 |
|---------|------|------|
| 配置目录 | `NANOBOT_CONFIG_DIR` | 测试配置与生产配置完全分离 |
| 数据目录 | `NANOBOT_DATA_DIR` | 测试数据与生产数据完全分离 |
| Workspace | 独立目录 | 测试工作区独立 |

### 2.2 快速准备（PowerShell）

```powershell
# 1. 清理并创建测试目录
if (Test-Path ".nanobot-runner-uat") { Remove-Item -Recurse -Force ".nanobot-runner-uat" }
New-Item -ItemType Directory -Path ".nanobot-runner-uat\test-data" -Force
New-Item -ItemType Directory -Path ".nanobot-runner-uat\data" -Force

# 2. 复制真实 FIT 文件（15-20个，覆盖所有类型）
Copy-Item "D:\yecll\Downloads\coros\test-fit-files\*.fit" -Destination ".nanobot-runner-uat\test-data\" -Recurse

# 3. 设置环境变量
$env:NANOBOT_CONFIG_DIR="$PWD\.nanobot-runner-uat"
$env:NANOBOT_DATA_DIR="$PWD\.nanobot-runner-uat\data"
$env:NANOBOT_WORKSPACE_DIR="$PWD\.nanobot-runner-uat"
$env:PYTHONIOENCODING="utf-8"

# 4. 初始化并验证
uv run nanobotrun system init
uv run nanobotrun system validate
```

### 2.3 测试数据要求

| 数据类型 | 数量 | 用途 |
|---------|------|------|
| 轻松跑 | ≥5 | 基础功能测试 |
| 节奏跑 | ≥5 | 心率数据分析 |
| 长距离跑 | ≥5 | VDOT 计算（≥1500m） |
| 间歇跑 | ≥5 | 训练负荷分析 |
| 异常文件 | 2 | 错误处理测试 |

---

## 3. 测试用例速查表

### 3.1 数据导入测试（UAT-001 ~ UAT-005）

| 编号 | 测试项 | 命令 | 通过标准 | 优先级 |
|------|--------|------|---------|--------|
| UAT-001 | 单文件导入 | `data import <file>` | 退出码0，输出"导入成功" | P0 |
| UAT-002 | 批量导入 | `data import <dir>` | 退出码0，输出"导入完成" | P0 |
| UAT-003 | 数据去重 | 重复导入同一文件 | 输出"跳过"或"已存在" | P0 |
| UAT-004 | 强制重新导入 | `data import <file> --force` | 退出码0，"导入成功" | P1 |
| UAT-005 | 异常文件处理 | `data import <empty/corrupted>` | 退出码1，含"错误:"和"建议:" | P1 |

### 3.2 数据查询测试（UAT-006 ~ UAT-008）

| 编号 | 测试项 | 命令 | 通过标准 | 优先级 |
|------|--------|------|---------|--------|
| UAT-006 | 当前年份统计 | `data stats` | 退出码0，含统计信息 | P0 |
| UAT-007 | 指定年份统计 | `data stats --year 2024` | 退出码0，含指定年份数据 | P0 |
| UAT-008 | 日期范围过滤 | `data stats --start 2024-01-01 --end 2024-12-31` | 退出码0，含日期范围数据 | P0 |

### 3.3 数据分析测试（UAT-009 ~ UAT-014）

| 编号 | 测试项 | 命令 | 通过标准 | 优先级 |
|------|--------|------|---------|--------|
| UAT-009 | VDOT 趋势 | `analysis vdot` | 退出码0，含 VDOT 数值 | P0 |
| UAT-010 | VDOT 限制 | `analysis vdot --limit 20` | 退出码0，显示≤20条 | P0 |
| UAT-011 | 训练负荷 | `analysis load` | 退出码0，含 ATL/CTL/TSB | P0 |
| UAT-012 | 负荷天数 | `analysis load --days 60` | 退出码0，含60天数据 | P0 |
| UAT-013 | 心率漂移 | `analysis hr-drift` | 退出码0，含相关性系数 | P0 |
| UAT-014 | 漂移判定 | 分析带心率数据记录 | 相关性<-0.7 判定为漂移 | P0 |

### 3.4 训练计划测试（UAT-015 ~ UAT-017）

| 编号 | 测试项 | 命令 | 通过标准 | 优先级 |
|------|--------|------|---------|--------|
| UAT-015 | 智能建议 | `plan advice <plan_id>` | 退出码0，含训练建议 | P0 |
| UAT-016 | 目标评估 | `plan evaluate <plan_id>` | 退出码0，含达成概率 | P0 |
| UAT-017 | 长期规划 | `plan long-term <distance> <date>` | 退出码0，含多周期计划 | P0 |

### 3.5 报告生成测试（UAT-018 ~ UAT-020）

| 编号 | 测试项 | 命令 | 通过标准 | 优先级 |
|------|--------|------|---------|--------|
| UAT-018 | 周报生成 | `report generate --type weekly` | 退出码0，含周报内容 | P0 |
| UAT-019 | 月报生成 | `report generate --type monthly` | 退出码0，含月报内容 | P0 |
| UAT-020 | 报告导出 | `report generate --type weekly --output report.md` | 文件生成成功 | P0 |

### 3.6 系统管理测试（UAT-021 ~ UAT-022）

| 编号 | 测试项 | 命令 | 通过标准 | 优先级 |
|------|--------|------|---------|--------|
| UAT-021 | 配置验证 | `system validate` | 退出码0，"配置验证通过" | P1 |
| UAT-022 | 数据迁移 | `system init --force` | 退出码0，"初始化完成" | P1 |

### 3.7 Agent 交互测试（UAT-023 ~ UAT-024）⚠️ 手动测试

| 编号 | 测试项 | 操作 | 通过标准 | 优先级 |
|------|--------|------|---------|--------|
| UAT-023 | 启动对话 | `agent chat` | Agent 正常启动，显示欢迎信息 | P1 |
| UAT-024 | 自然语言查询 | 输入"我上周跑了多少次？" | 返回准确数据，调用对应工具 | P1 |

### 3.8 性能测试（UAT-025 ~ UAT-027）

| 编号 | 测试项 | 命令 | 性能要求 | 优先级 |
|------|--------|------|---------|--------|
| UAT-025 | 批量导入 | `data import <50-100 files>` | 50文件<30s，100文件<60s | P1 |
| UAT-026 | 数据查询 | `data stats` | 100条<2s，500条<5s | P1 |
| UAT-027 | 报告生成 | `report weekly` | 周报<5s，月报<8s | P1 |

### 3.9 MCP 工具管理测试（UAT-028 ~ UAT-034）

| 编号 | 测试项 | 命令 | 通过标准 | 优先级 |
|------|--------|------|---------|--------|
| UAT-028 | 列出工具 | `tools list` | 退出码0，显示工具列表 | P0 |
| UAT-029 | 添加服务器 | `tools add weather --url <url>` | 退出码0，"添加成功" | P0 |
| UAT-030 | 启用/禁用 | `tools enable/disable weather` | 退出码0，状态变更 | P0 |
| UAT-031 | 移除服务器 | `tools remove weather` | 退出码0，"移除成功" | P1 |
| UAT-032 | 验证配置 | `tools validate` | 退出码0，"配置验证通过" | P1 |
| UAT-033 | 导入 Claude 配置 | `tools import-claude` | 退出码0，"导入成功" | P1 |
| UAT-034 | 自动发现 | `tools list` + Agent 启动 | 工具自动注册到 Agent | P1 |

### 3.10 Agent 工具集成测试（UAT-035 ~ UAT-037）⚠️ 手动测试

| 编号 | 测试项 | 测试输入 | 通过标准 | 优先级 |
|------|--------|---------|---------|--------|
| UAT-035 | 天气工具 | "明天北京天气如何？" | 返回天气信息，给出训练建议 | P1 |
| UAT-036 | 地图工具 | "推荐一条5km的跑步路线" | 返回路线规划，含距离/海拔 | P1 |
| UAT-037 | 健康数据工具 | "分析我的HRV趋势" | 返回HRV数据，综合评估 | P1 |

### 3.11 Gateway 服务测试（UAT-038 ~ UAT-042）

| 编号 | 测试项 | 命令/操作 | 通过标准 | 优先级 |
|------|--------|----------|---------|--------|
| UAT-038 | 服务启动 | `gateway start --port 18791` | 启动成功，显示通道列表 | P0 |
| UAT-039 | 命令路由 /stats | 飞书发送 `/stats` | 返回训练统计信息 | P0 |
| UAT-040 | 命令路由 /vd | 飞书发送 `/vd` | 返回 VDOT 趋势数据 | P0 |
| UAT-041 | 自然语言处理 | 飞书发送"我最近跑得怎么样？" | 返回自然语言回答 | P1 |
| UAT-042 | 服务停止 | `Ctrl+C` | 正常退出，退出码0 | P1 |

---

## 4. 验收标准

### 4.1 通过标准

| 条件 | 要求 | 验证方式 |
|------|------|---------|
| P0 用例通过率 | 100%（排除已知跳过用例） | 统计 P0 用例通过数/总数 |
| P1 用例通过率 | ≥90% | 统计 P1 用例通过数/总数 |
| 致命/严重 Bug | 0 个 | Bug 清单统计 |
| 核心业务流程 | 全量闭环 | 业务流程检查 |
| 数据计算准确性 | VDOT/TSS/心率漂移计算正确 | 与手工计算对比 |
| 性能指标 | 符合性能要求 | 性能测试用例验证 |

### 4.2 不通过标准

- P0 用例通过率 < 100%（排除已知跳过用例）
- 存在致命或严重级 Bug
- 核心业务流程未闭环
- 数据计算存在明显错误

---

## 5. 详细用例与自动化脚本

**完整测试用例详情**（含自动化判定规则、结果记录模板、环境配置脚本）：
[docs/test/uat_test_cases/README.md](docs/test/uat_test_cases/README.md)

**按模块拆分的详细用例**：
- [数据导入与查询](docs/test/uat_test_cases/UAT_data_import.md) - UAT-001 ~ UAT-008
- [数据分析](docs/test/uat_test_cases/UAT_data_analysis.md) - UAT-009 ~ UAT-014
- [训练计划与报告](docs/test/uat_test_cases/UAT_planning_report.md) - UAT-015 ~ UAT-020
- [系统管理与性能](docs/test/uat_test_cases/UAT_system_performance.md) - UAT-021 ~ UAT-027
- [MCP 工具与 Gateway](docs/test/uat_test_cases/UAT_tools_gateway.md) - UAT-028 ~ UAT-042
