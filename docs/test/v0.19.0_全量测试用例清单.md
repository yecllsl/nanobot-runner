# v0.19.0 全量测试用例清单

**版本**: v0.19.0  
**创建日期**: 2026-05-06  
**状态**: 待执行  
**负责人**: 测试工程师  
**关联文档**: [v0.19.0_测试策略方案.md](./v0.19.0_测试策略方案.md)

---

## 一、测试用例汇总

| 测试模块 | 用例数量 | P0 | P1 | 对应测试文件 |
|---------|---------|-----|-----|-------------|
| HRVAnalyzer | 12 | 10 | 2 | `tests/unit/core/body_signal/test_hrv_analyzer.py` |
| FatigueAssessor | 10 | 10 | 0 | `tests/unit/core/body_signal/test_fatigue_assessor.py` |
| RecoveryMonitor | 7 | 5 | 2 | `tests/unit/core/body_signal/test_recovery_monitor.py` |
| BodySignalEngine | 10 | 10 | 0 | `tests/unit/core/body_signal/test_body_signal_engine.py` |
| BodySignalConfig | 3 | 2 | 1 | `tests/unit/core/body_signal/test_body_signal_config.py` |
| 集成测试 | 6 | 6 | 0 | `tests/integration/test_body_signal_integration.py` |
| 场景级测试 | 4 | 3 | 1 | 新增/扩展现有测试文件 |
| 回归测试 | 5 | 5 | 0 | 对应已有测试文件 |
| **总计** | **57** | **51** | **6** | - |

---

## 二、HRVAnalyzer 单元测试 (12条)

**测试文件**: `tests/unit/core/body_signal/test_hrv_analyzer.py`

### HRV-UT-01: test_analyze_hrv_success
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 有30天心率数据
- **测试步骤**:
  1. Mock SessionRepository返回30天心率数据
  2. 调用 `HRVAnalyzer.analyze_hrv(days=30)`
- **预期结果**:
  - 返回完整 `HRVAnalysisResult`
  - `data_quality = DataQuality.SUFFICIENT`
  - `resting_hr_trend` 包含30个数据点
  - 包含趋势分析和偏离度计算

### HRV-UT-02: test_analyze_hrv_empty_data
- **类型**: 边界测试
- **优先级**: P0
- **前置条件**: 无心率数据
- **测试步骤**:
  1. Mock SessionRepository返回空列表
  2. 调用 `HRVAnalyzer.analyze_hrv(days=30)`
- **预期结果**:
  - `data_quality = DataQuality.EMPTY`
  - `resting_hr_trend = []`
  - `trend_analysis = "无数据"`

### HRV-UT-03: test_analyze_hrv_insufficient_data
- **类型**: 边界测试
- **优先级**: P0
- **前置条件**: 仅3天心率数据
- **测试步骤**:
  1. Mock SessionRepository返回3天数据
  2. 调用 `HRVAnalyzer.analyze_hrv(days=30)`
- **预期结果**:
  - `data_quality = DataQuality.INSUFFICIENT`
  - `warning_message` 包含"数据不足"

### HRV-UT-04: test_get_resting_hr_trend_7_days
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 有7天心率数据
- **测试步骤**:
  1. 准备7天心率数据
  2. 调用 `get_resting_hr_trend(days=7)`
- **预期结果**:
  - 返回7个 `RestingHRPoint` 对象
  - 每个点包含最低10%心率均值

### HRV-UT-05: test_get_resting_hr_trend_30_days
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 有30天心率数据
- **测试步骤**:
  1. 准备30天心率数据
  2. 调用 `get_resting_hr_trend(days=30)`
- **预期结果**:
  - 返回最多30个数据点
  - 计算每个点相对于平均值的 `deviation_pct`

### HRV-UT-06: test_get_resting_hr_trend_single_point
- **类型**: 边界测试
- **优先级**: P0
- **前置条件**: 仅1天心率数据
- **测试步骤**:
  1. 准备1天心率数据
  2. 调用 `get_resting_hr_trend(days=30)`
- **预期结果**:
  - 返回1个数据点
  - `deviation_pct = 0.0` (无对比基准)

### HRV-UT-07: test_analyze_hr_recovery_calculation
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 有完整session数据（含逐秒心率）
- **测试步骤**:
  1. 准备包含逐秒心率数据的session
  2. 调用 `analyze_hr_recovery(session_id, minutes_after=5)`
- **预期结果**:
  - `recovery_rate` 计算正确
  - 公式: `(运动末HR - N分钟后HR) / (运动末HR - 静息HR) x 100%`

### HRV-UT-08: test_analyze_hr_recovery_no_second_data
- **类型**: 边界测试
- **优先级**: P1
- **前置条件**: 无逐秒心率数据
- **测试步骤**:
  1. 准备不含逐秒心率的session
  2. 调用 `analyze_hr_recovery(session_id)`
- **预期结果**:
  - `data_quality = DataQuality.INSUFFICIENT`
  - 返回提示信息

### HRV-UT-09: test_check_hr_drift_alert_triggered
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 心率漂移率15%
- **测试步骤**:
  1. Mock心率漂移率=15%
  2. 调用 `check_hr_drift(session_id)`
- **预期结果**:
  - `drift_alert = True`
  - `message` 包含"降低配速"或"注意补水"

### HRV-UT-10: test_check_hr_drift_normal
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 心率漂移率5%
- **测试步骤**:
  1. Mock心率漂移率=5%
  2. 调用 `check_hr_drift(session_id)`
- **预期结果**:
  - `drift_alert = False`
  - `message = "心率漂移正常"`

### HRV-UT-11: test_estimate_hrv_no_rr_interval
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 无RR间期列
- **测试步骤**:
  1. 准备不含rr_interval列的数据
  2. 调用 `estimate_hrv(data)`
- **预期结果**:
  - `data_source = HRVDataSource.HR_ESTIMATE`
  - `rmssd = None`
  - 标注"非医疗级精度"

### HRV-UT-12: test_estimate_hrv_with_rr_interval
- **类型**: 功能测试
- **优先级**: P1
- **前置条件**: 有rr_interval列
- **测试步骤**:
  1. 准备包含rr_interval列的数据
  2. 调用 `estimate_hrv(data)`
- **预期结果**:
  - `data_source = HRVDataSource.RR_INTERVAL`
  - `rmssd > 0`

---

## 三、FatigueAssessor 单元测试 (10条)

**测试文件**: `tests/unit/core/body_signal/test_fatigue_assessor.py`

### FAT-UT-01: test_assess_fatigue_full_score
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 完整数据+RPE=7
- **测试步骤**:
  1. Mock完整训练数据（TSB、ATL、CTL、RPE）
  2. 调用 `assess_fatigue(rpe=7)`
- **预期结果**:
  - `fatigue_score` 在 0-100 之间
  - 返回完整疲劳评估结果

### FAT-UT-02: test_assess_fatigue_empty_data
- **类型**: 边界测试
- **优先级**: P0
- **前置条件**: 无训练数据
- **测试步骤**:
  1. Mock空数据
  2. 调用 `assess_fatigue()`
- **预期结果**:
  - `fatigue_score = 0.0`
  - `recovery_status = RecoveryStatus.GREEN`
  - `data_quality = DataQuality.EMPTY`

### FAT-UT-03: test_recovery_status_green
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: TSB=15, fatigue=20
- **测试步骤**:
  1. 设置 TSB=15, fatigue_score=20
  2. 调用 `get_recovery_status()`
- **预期结果**:
  - `recovery_status = RecoveryStatus.GREEN`
  - 状态描述为"恢复良好"

### FAT-UT-04: test_recovery_status_yellow
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: TSB=5, fatigue=40
- **测试步骤**:
  1. 设置 TSB=5, fatigue_score=40
  2. 调用 `get_recovery_status()`
- **预期结果**:
  - `recovery_status = RecoveryStatus.YELLOW`
  - 状态描述为"注意恢复"

### FAT-UT-05: test_recovery_status_red_low_tsb
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: TSB=-10
- **测试步骤**:
  1. 设置 TSB=-10
  2. 调用 `get_recovery_status()`
- **预期结果**:
  - `recovery_status = RecoveryStatus.RED`
  - 状态描述为"过度训练风险"

### FAT-UT-06: test_recovery_status_red_high_fatigue
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: fatigue=70
- **测试步骤**:
  1. 设置 fatigue_score=70
  2. 调用 `get_recovery_status()`
- **预期结果**:
  - `recovery_status = RecoveryStatus.RED`

### FAT-UT-07: test_consecutive_hard_days_4_plus
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 7天中4天TSS>80
- **测试步骤**:
  1. Mock 7天内4天TSS>80
  2. 调用 `check_consecutive_hard_days()`
- **预期结果**:
  - 返回警告
  - 建议文案包含"连续高强度训练过多"

### FAT-UT-08: test_rpe_weight_redistribution
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 无RPE输入
- **测试步骤**:
  1. 不提供RPE值
  2. 调用疲劳度计算
- **预期结果**:
  - 主观权重重新分配到其他三个维度
  - 权重总和仍为100%

### FAT-UT-09: test_tsb_clamping
- **类型**: 边界测试
- **优先级**: P0
- **前置条件**: TSB=60
- **测试步骤**:
  1. 设置 TSB=60
  2. 调用疲劳度计算
- **预期结果**:
  - TSB 被截断至最大值 50

### FAT-UT-10: test_invalid_rpe_raises_error
- **类型**: 异常测试
- **优先级**: P0
- **前置条件**: RPE=15 (无效值)
- **测试步骤**:
  1. 设置 RPE=15
  2. 调用 `assess_fatigue(rpe=15)`
- **预期结果**:
  - 抛出 `ValueError`

---

## 四、RecoveryMonitor 单元测试 (7条)

**测试文件**: `tests/unit/core/body_signal/test_recovery_monitor.py`

### REC-UT-01: test_get_recovery_status_sufficient
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 完整恢复数据
- **测试步骤**:
  1. Mock完整恢复数据
  2. 调用 `get_recovery_status()`
- **预期结果**:
  - `data_quality = DataQuality.SUFFICIENT`
  - 包含 `recovery_trend` 趋势数据

### REC-UT-02: test_get_recovery_status_empty
- **类型**: 边界测试
- **优先级**: P0
- **前置条件**: 无恢复数据
- **测试步骤**:
  1. Mock空数据
  2. 调用 `get_recovery_status()`
- **预期结果**:
  - `data_quality = DataQuality.EMPTY`
  - `recovery_status = RecoveryStatus.GREEN`

### REC-UT-03: test_rest_day_effect_good_hr
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 静息心率下降6%
- **测试步骤**:
  1. Mock休息前后静息心率下降6%
  2. 调用 `analyze_rest_day_effect()`
- **预期结果**:
  - `effect_level = "good"`

### REC-UT-04: test_rest_day_effect_good_tsb
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: TSB上升12
- **测试步骤**:
  1. Mock休息后TSB从-5上升到7
  2. 调用 `analyze_rest_day_effect()`
- **预期结果**:
  - `effect_level = "good"`

### REC-UT-05: test_rest_day_effect_minimal
- **类型**: 边界测试
- **优先级**: P1
- **前置条件**: 心率变化1%, TSB变化2
- **测试步骤**:
  1. Mock微小变化
  2. 调用 `analyze_rest_day_effect()`
- **预期结果**:
  - `effect_level = "minimal"`

### REC-UT-06: test_get_recovery_trend_7_days
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 有7天恢复数据
- **测试步骤**:
  1. Mock 7天恢复数据
  2. 调用 `get_recovery_trend(days=7)`
- **预期结果**:
  - 返回按日期排序的 `RecoveryPoint` 列表
  - 包含7个数据点

### REC-UT-07: test_get_recovery_trend_insufficient
- **类型**: 边界测试
- **优先级**: P1
- **前置条件**: 仅2天恢复数据
- **测试步骤**:
  1. Mock 2天恢复数据
  2. 调用 `get_recovery_trend(days=7)`
- **预期结果**:
  - 返回2个数据点
  - 提示数据不足

---

## 五、BodySignalEngine 单元测试 (10条)

**测试文件**: `tests/unit/core/body_signal/test_body_signal_engine.py`

### ENG-UT-01: test_get_daily_summary_full
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 完整身体信号数据
- **测试步骤**:
  1. Mock完整数据（HRV、疲劳度、恢复状态）
  2. 调用 `get_daily_summary(date=today)`
- **预期结果**:
  - 返回包含emoji状态标识
  - 包含静息心率、疲劳度评分
  - 包含具体训练建议

### ENG-UT-02: test_get_daily_summary_empty
- **类型**: 边界测试
- **优先级**: P0
- **前置条件**: 无身体信号数据
- **测试步骤**:
  1. Mock空数据
  2. 调用 `get_daily_summary()`
- **预期结果**:
  - `daily_summary = "暂无数据"`
  - `recommendation = "请先导入训练记录"`

### ENG-UT-03: test_get_daily_summary_cache
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 同日多次调用
- **测试步骤**:
  1. 第一次调用 `get_daily_summary()`
  2. 第二次调用 `get_daily_summary()`
- **预期结果**:
  - 第二次返回缓存结果
  - 不重复计算

### ENG-UT-04: test_get_daily_summary_cache_expire
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 跨日调用
- **测试步骤**:
  1. Day1 调用并缓存
  2. Day2 再次调用
- **预期结果**:
  - 缓存失效
  - 重新计算并返回新结果

### ENG-UT-05: test_get_weekly_summary_with_comparison
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 14天身体信号数据
- **测试步骤**:
  1. Mock 14天数据
  2. 调用 `get_weekly_summary()`
- **预期结果**:
  - 包含"较上周"对比文本
  - 展示趋势变化

### ENG-UT-06: test_alert_hr_spike
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 静息心率偏差15%
- **测试步骤**:
  1. Mock静息心率比平均值高15%
  2. 检查预警触发
- **预期结果**:
  - 触发 `warning` 级别 `hr_spike` 预警
  - 预警消息包含"静息心率突增"

### ENG-UT-07: test_alert_overtraining
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 连续3天TSB<-20
- **测试步骤**:
  1. Mock连续3天TSB=-25
  2. 检查预警触发
- **预期结果**:
  - 触发 `critical` 级别 `overtraining` 预警
  - 预警消息包含"过度训练风险"

### ENG-UT-08: test_alert_fatigue_rising
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 连续3天TSB递减
- **测试步骤**:
  1. Mock TSB: -10 → -15 → -20
  2. 检查预警触发
- **预期结果**:
  - 触发 `warning` 级别 `fatigue_rising` 预警

### ENG-UT-09: test_training_advice_critical_alert
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 有critical级别预警
- **测试步骤**:
  1. 设置critical预警
  2. 调用训练建议生成
- **预期结果**:
  - 建议包含"完全休息"
  - 不建议进行训练

### ENG-UT-10: test_training_advice_green_status
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: GREEN恢复状态
- **测试步骤**:
  1. 设置 `recovery_status = GREEN`
  2. 调用训练建议生成
- **预期结果**:
  - 建议包含"质量课训练"
  - 可进行高强度训练

---

## 六、BodySignalConfig 单元测试 (3条)

**测试文件**: `tests/unit/core/body_signal/test_body_signal_config.py`

### CFG-UT-01: test_default_config_valid
- **类型**: 功能测试
- **优先级**: P0
- **前置条件**: 使用默认参数
- **测试步骤**:
  1. 创建默认 `BodySignalConfig`
- **预期结果**:
  - 权重总和 = 100%
  - 不抛出异常

### CFG-UT-02: test_invalid_weights_raises
- **类型**: 异常测试
- **优先级**: P0
- **前置条件**: 权重和=120%
- **测试步骤**:
  1. 创建配置，设置权重和=120%
- **预期结果**:
  - 抛出 `ValueError`

### CFG-UT-03: test_custom_config_valid
- **类型**: 功能测试
- **优先级**: P1
- **前置条件**: 自定义权重和=100%
- **测试步骤**:
  1. 创建自定义配置
- **预期结果**:
  - 正常创建，无异常

---

## 七、集成测试 (6条)

**测试文件**: `tests/integration/test_body_signal_integration.py`

### ITG-01: test_body_signal_end_to_end
- **类型**: 集成测试
- **优先级**: P0
- **覆盖范围**: Parquet → HRV → Fatigue → Engine → Summary
- **测试步骤**:
  1. 从Parquet读取数据
  2. 执行HRV分析
  3. 评估疲劳度
  4. 生成身体状态摘要
- **预期结果**:
  - 完整流程输出 `BodySignalSummary`
  - 各环节数据传递正确

### ITG-02: test_cli_status_today
- **类型**: CLI集成测试
- **优先级**: P0
- **测试步骤**:
  1. 执行 `nanobotrun status today`
- **预期结果**:
  - 输出快速摘要
  - 响应时间 < 500ms

### ITG-03: test_cli_analysis_hrv
- **类型**: CLI集成测试
- **优先级**: P0
- **测试步骤**:
  1. 执行 `nanobotrun analysis hrv --days 30`
- **预期结果**:
  - 输出完整HRV分析结果
  - 格式正确

### ITG-04: test_cli_analysis_fatigue
- **类型**: CLI集成测试
- **优先级**: P0
- **测试步骤**:
  1. 执行 `nanobotrun analysis fatigue`
- **预期结果**:
  - 输出疲劳度评分
  - 输出恢复建议

### ITG-05: test_agent_tools_body_signal
- **类型**: Agent集成测试
- **优先级**: P0
- **覆盖范围**: 6个新增Agent工具
- **测试步骤**:
  1. 调用 `get_hrv_analysis`
  2. 调用 `get_hr_recovery`
  3. 调用 `get_fatigue_score`
  4. 调用 `get_recovery_status`
  5. 调用 `get_body_signal_summary`
  6. 调用 `compare_training_periods`
- **预期结果**:
  - 所有工具返回正确JSON格式
  - 数据准确

### ITG-06: test_lazyframe_no_early_collect
- **类型**: 架构测试
- **优先级**: P0
- **测试步骤**:
  1. 执行HRVAnalyzer查询
  2. 检查中间查询是否使用LazyFrame
- **预期结果**:
  - 中间查询使用LazyFrame
  - 仅在最终输出时调用 `.collect()`

---

## 八、场景级集成测试 (4条)

### SCENE-01: test_full_workflow_import_to_summary
- **类型**: 场景测试
- **优先级**: P0
- **场景**: 导入FIT → 分析 → 生成身体状态摘要
- **测试步骤**:
  1. 导入测试FIT文件
  2. 执行HRV分析
  3. 生成身体状态摘要
- **预期结果**:
  - 全流程闭环
  - 无数据丢失

### SCENE-02: test_degradation_no_data
- **类型**: 降级测试
- **优先级**: P0
- **场景**: 首次使用，无任何数据
- **测试步骤**:
  1. 在空数据环境下调用所有分析器
- **预期结果**:
  - 所有分析器返回 `EMPTY` 状态
  - 返回友好提示信息

### SCENE-03: test_degradation_insufficient_data
- **类型**: 降级测试
- **优先级**: P0
- **场景**: 仅1天数据
- **测试步骤**:
  1. 准备仅1天数据
  2. 调用所有分析器
- **预期结果**:
  - 所有分析器返回 `INSUFFICIENT` 状态
  - 标注"仅供参考"

### SCENE-04: test_consecutive_query_performance
- **类型**: 性能测试
- **优先级**: P1
- **场景**: 连续10次查询身体状态
- **测试步骤**:
  1. 连续10次调用 `get_daily_summary()`
  2. 记录每次响应时间
- **预期结果**:
  - 利用缓存机制
  - 每次响应时间 < 100ms

---

## 九、回归测试 (5条)

### REG-01: test_existing_vdot_calculation
- **类型**: 回归测试
- **优先级**: P0
- **验证范围**: VDOT计算不受v0.19.0影响
- **测试步骤**:
  1. 使用相同测试数据
  2. 执行VDOT计算
- **预期结果**:
  - 结果与v0.18.0一致

### REG-02: test_existing_training_load
- **类型**: 回归测试
- **优先级**: P0
- **验证范围**: ATL/CTL/TSB计算不受影响
- **测试步骤**:
  1. 使用相同测试数据
  2. 执行训练负荷计算
- **预期结果**:
  - 结果与v0.18.0一致

### REG-03: test_existing_data_import
- **类型**: 回归测试
- **优先级**: P0
- **验证范围**: FIT导入流程不受影响
- **测试步骤**:
  1. 执行FIT导入
  2. 检查SHA256去重
- **预期结果**:
  - SHA256去重正常工作

### REG-04: test_existing_cli_commands
- **类型**: 回归测试
- **优先级**: P0
- **验证范围**: 已有CLI命令正常工作
- **测试步骤**:
  1. 执行所有已有CLI命令
- **预期结果**:
  - 无命令冲突
  - 输出格式正确

### REG-05: test_existing_agent_tools
- **类型**: 回归测试
- **优先级**: P0
- **验证范围**: 已有Agent工具正常工作
- **测试步骤**:
  1. 调用所有已有Agent工具
- **预期结果**:
  - 无工具冲突
  - 返回数据正确

---

## 十、测试执行记录

| 用例ID | 执行状态 | 执行结果 | Bug ID | 备注 |
|--------|---------|---------|--------|------|
| HRV-UT-01 | 待执行 | - | - | - |
| HRV-UT-02 | 待执行 | - | - | - |
| HRV-UT-03 | 待执行 | - | - | - |
| HRV-UT-04 | 待执行 | - | - | - |
| HRV-UT-05 | 待执行 | - | - | - |
| HRV-UT-06 | 待执行 | - | - | - |
| HRV-UT-07 | 待执行 | - | - | - |
| HRV-UT-08 | 待执行 | - | - | - |
| HRV-UT-09 | 待执行 | - | - | - |
| HRV-UT-10 | 待执行 | - | - | - |
| HRV-UT-11 | 待执行 | - | - | - |
| HRV-UT-12 | 待执行 | - | - | - |
| FAT-UT-01 | 待执行 | - | - | - |
| FAT-UT-02 | 待执行 | - | - | - |
| FAT-UT-03 | 待执行 | - | - | - |
| FAT-UT-04 | 待执行 | - | - | - |
| FAT-UT-05 | 待执行 | - | - | - |
| FAT-UT-06 | 待执行 | - | - | - |
| FAT-UT-07 | 待执行 | - | - | - |
| FAT-UT-08 | 待执行 | - | - | - |
| FAT-UT-09 | 待执行 | - | - | - |
| FAT-UT-10 | 待执行 | - | - | - |
| REC-UT-01 | 待执行 | - | - | - |
| REC-UT-02 | 待执行 | - | - | - |
| REC-UT-03 | 待执行 | - | - | - |
| REC-UT-04 | 待执行 | - | - | - |
| REC-UT-05 | 待执行 | - | - | - |
| REC-UT-06 | 待执行 | - | - | - |
| REC-UT-07 | 待执行 | - | - | - |
| ENG-UT-01 | 待执行 | - | - | - |
| ENG-UT-02 | 待执行 | - | - | - |
| ENG-UT-03 | 待执行 | - | - | - |
| ENG-UT-04 | 待执行 | - | - | - |
| ENG-UT-05 | 待执行 | - | - | - |
| ENG-UT-06 | 待执行 | - | - | - |
| ENG-UT-07 | 待执行 | - | - | - |
| ENG-UT-08 | 待执行 | - | - | - |
| ENG-UT-09 | 待执行 | - | - | - |
| ENG-UT-10 | 待执行 | - | - | - |
| CFG-UT-01 | 待执行 | - | - | - |
| CFG-UT-02 | 待执行 | - | - | - |
| CFG-UT-03 | 待执行 | - | - | - |
| ITG-01 | 待执行 | - | - | - |
| ITG-02 | 待执行 | - | - | - |
| ITG-03 | 待执行 | - | - | - |
| ITG-04 | 待执行 | - | - | - |
| ITG-05 | 待执行 | - | - | - |
| ITG-06 | 待执行 | - | - | - |
| SCENE-01 | 待执行 | - | - | - |
| SCENE-02 | 待执行 | - | - | - |
| SCENE-03 | 待执行 | - | - | - |
| SCENE-04 | 待执行 | - | - | - |
| REG-01 | 待执行 | - | - | - |
| REG-02 | 待执行 | - | - | - |
| REG-03 | 待执行 | - | - | - |
| REG-04 | 待执行 | - | - | - |
| REG-05 | 待执行 | - | - | - |

---

**状态**: 测试用例清单已创建，等待执行。
