# 回归测试报告 v0.22.0

> **测试日期**: 2026-05-15
> **测试者**: 测试工程师
> **版本**: v0.22.0
> **前置依赖**: [Bug修复报告_v0.22.0.md](../../development/Bug修复报告_v0.22.0.md)

---

## 一、测试范围

### 1.1 修复的 Bug 列表

| Bug ID | 严重等级 | 模块 | 修复方案 |
|--------|---------|------|---------|
| BUG-2201 | 严重 | 配置/Cron | ConfigManager.get() 改用 load_config_with_env_override() |
| BUG-2202 | 严重 | 身体信号 | fatigue_assessor/recovery_monitor 从 session_repo 读取数据 |
| BUG-2203 | 严重 | 数字孪生 | StateVectorBuilder.build_load() 从 session_repo 读取数据 |

### 1.2 修复的代码范围

| 源文件 | 修改内容 |
|--------|---------|
| `src/core/config/manager.py` | get() 方法改用环境变量覆盖读取 |
| `src/core/body_signal/fatigue_assessor.py` | assess_fatigue() 从 Parquet 读取数据 |
| `src/core/body_signal/recovery_monitor.py` | get_recovery_status() 从 Parquet 读取数据 |
| `src/core/twin/state_vector_builder.py` | build_load() 从 Parquet 读取数据 |

### 1.3 受影响的功能范围

| 受影响模块 | 影响说明 |
|-----------|---------|
| ConfigManager 所有调用方 | 环境变量覆盖生效，Cron/飞书等配置可被环境变量覆盖 |
| FatigueAssessor | 疲劳度评估基于真实训练数据，不再返回"暂无数据" |
| RecoveryMonitor | 恢复状态评估基于真实训练数据，不再返回"暂无数据" |
| StateVectorBuilder | 数字孪生负荷维度 CTL/ATL 基于真实数据计算 |
| DigitalTwinEngine | 间接受益，状态向量构建更准确 |

---

## 二、回归测试结果

### 2.1 Bug 修复验证

| 测试用例 | 验证内容 | 结果 |
|---------|---------|------|
| `test_get_reads_env_override` | 环境变量覆盖配置文件值 | ✅ 通过 |
| `test_get_env_override_bool_type` | 布尔类型环境变量转换 | ✅ 通过 |
| `test_get_env_override_falls_back_to_file` | 无环境变量时回退到配置文件 | ✅ 通过 |
| `test_assess_fatigue_reads_session_data` | fatigue从session_repo读取数据 | ✅ 通过 |
| `test_assess_fatigue_read_parquet_exception` | read_parquet异常时回退 | ✅ 通过 |
| `test_get_recovery_status_reads_session_data` | recovery从session_repo读取数据 | ✅ 通过 |
| `test_get_recovery_status_read_parquet_exception` | read_parquet异常时回退 | ✅ 通过 |
| `test_build_load_with_session_data` | 从session_repo读取数据计算CTL/ATL | ✅ 通过 |
| `test_build_load_without_session_repo` | 无session_repo时回退到calculate_atl_ctl | ✅ 通过 |
| `test_build_load_read_parquet_exception` | read_parquet异常时回退到空DataFrame | ✅ 通过 |

**Bug 修复验证通过率**: 10/10 = **100%**

### 2.2 受影响功能测试

| 测试范围 | 用例数 | 通过 | 失败 | 通过率 |
|---------|-------|------|------|-------|
| Config 模块 | 全部 | 全部通过 | 0 | 100% |
| Body Signal 模块 | 全部 | 全部通过 | 0 | 100% |
| Twin 模块 | 全部 | 250通过 | 1(预存) | 99.6% |
| **合计** | **252** | **251** | **1(预存)** | **99.6%** |

### 2.3 修复前后对比

| 指标 | 修复前 | 修复后 | 变化 |
|------|-------|-------|------|
| 受影响模块测试通过数 | 241 | 251 | +10（新增回归测试） |
| 失败数（预存） | 1 | 1 | 无变化 |
| 新增测试用例 | 0 | 10 | +10 |

---

## 三、新 Bug 清单

### 3.1 本次发现的新 Bug

| Bug ID | 严重等级 | 模块 | 描述 | 状态 |
|--------|---------|------|------|------|
| 无 | - | - | 本次回归测试未发现新 Bug | - |

### 3.2 预存问题（非本次引入）

| 测试用例 | 问题描述 | 根因 | 建议 |
|---------|---------|------|------|
| `test_uses_memory_cache_on_second_call` | `_make_state()` 的 `snapshot_date="2026-05-12"` 已过期，导致缓存失效 | 测试数据硬编码日期，未使用动态日期 | 修改为使用 `datetime.now().strftime("%Y-%m-%d")` |

---

## 四、上线结论

### 4.1 上线评估

| 评估项 | 结果 | 标准 |
|-------|------|------|
| P0/P1 Bug 修复率 | 100% (3/3) | ≥100% |
| 回归测试通过率 | 100% (10/10) | ≥100% |
| 受影响功能通过率 | 99.6% (251/252) | ≥95% |
| 新 Bug 数量 | 0 | 0 |
| Lint 检查 | 通过 | 通过 |
| 类型检查 | 通过 | 通过 |

### 4.2 上线结论

**✅ 建议上线**

**理由**：
1. 3 个严重 Bug 全部修复，回归测试 100% 通过
2. 受影响功能测试通过率 99.6%，仅 1 个预存问题（与本次修复无关）
3. 未发现任何新 Bug
4. 代码质量检查（lint + mypy）全部通过

### 4.3 上线风险提示

| 风险项 | 风险等级 | 应对措施 |
|-------|---------|---------|
| `test_uses_memory_cache_on_second_call` 预存失败 | 低 | 不影响功能，仅测试数据过期，建议后续修复 |
| 环境变量覆盖可能影响现有配置 | 低 | 环境变量优先级高于配置文件，需确保部署环境正确配置 |

---

## 五、测试环境

| 项目 | 值 |
|------|-----|
| 操作系统 | Windows |
| Python 版本 | 3.11.15 |
| pytest 版本 | 9.0.2 |
| 测试命令 | `uv run pytest tests/unit/ -v --no-cov` |
