# v0.26.0 底座升级与新特性适配 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 安全升级 nanobot-ai 底座到 0.2.0 并适配 GoalState/推理可见化/Model Presets 三项新特性，零功能回归。

**Architecture:** 底座升级采用保守兼容策略（ADR-011），先升级依赖→全量测试→逐个修复→确认零回归后再适配新特性。GoalState 通过 SOUL.md 注入指导 + DecisionLogHook 读取 metadata 实现（ADR-012）。推理可见化通过 DecisionLogHook 重写 emit_reasoning() 实现（ADR-013）。Model Presets 通过 config.json 配置预设 + CLI 查看命令实现（ADR-014）。

**Tech Stack:** Python 3.11+, nanobot-ai 0.2.0, Polars, Typer/Rich CLI, Pydantic

---

## File Structure

| 操作 | 文件路径 | 职责 |
|------|----------|------|
| Modify | `pyproject.toml` | nanobot-ai 版本升级 |
| Modify | `src/core/evolution/models.py` | DecisionLog 新增 goal_state 字段 |
| Modify | `src/core/evolution/decision_log_hook.py` | 新增 emit_reasoning/goal_state_raw/推理缓冲区 |
| Modify | `src/core/evolution/evolution_controller.py` | 月度复盘新增 GoalState 检查 |
| Modify | `src/core/config/schema.py` | AppConfig 新增 model_presets 字段 |
| Modify | `templates/SOUL.md` | 新增 GoalState 使用指导段落 |
| Create | `src/cli/commands/model.py` | model list CLI 命令 |
| Create | `src/cli/handlers/model_handler.py` | model handler 业务逻辑 |
| Modify | `src/cli/commands/__init__.py` | 注册 model_app |
| Modify | `src/cli/app.py` | 注册 model 命令组 |
| Create | `tests/unit/core/evolution/test_goal_state.py` | GoalState 适配测试 |
| Create | `tests/unit/core/evolution/test_reasoning.py` | 推理可见化测试 |
| Create | `tests/unit/cli/test_model_cli.py` | Model Presets CLI 测试 |
| Modify | `tests/unit/core/evolution/test_models.py` | DecisionLog goal_state 字段测试 |
| Modify | `tests/unit/core/evolution/test_decision_log_hook.py` | Hook 扩展测试 |

---

## Task 1: 底座依赖升级

**Files:**
- Modify: `pyproject.toml:6`

- [ ] **Step 1: 修改 pyproject.toml 中的 nanobot-ai 版本**

将 `nanobot-ai>=0.1.5.post2` 修改为 `nanobot-ai>=0.2.0`：

```toml
dependencies = [
    "nanobot-ai>=0.2.0",
    "typer[all]>=0.12.0",
```

- [ ] **Step 2: 执行 uv sync 安装新版本依赖**

Run: `uv sync`
Expected: 成功安装 nanobot-ai 0.2.0 及其依赖，无依赖冲突错误

- [ ] **Step 3: 验证 nanobot-ai 版本**

Run: `uv run python -c "import nanobot; print(nanobot.__version__)"`
Expected: 输出 ≥ 0.2.0

- [ ] **Step 4: 验证 AgentHook 接口兼容性**

Run: `uv run python -c "from nanobot.agent.hook import AgentHook, AgentHookContext; print('AgentHook OK')"`
Expected: 输出 `AgentHook OK`

- [ ] **Step 5: 运行全量测试确认基线**

Run: `uv run pytest tests/ -x --timeout=60`
Expected: 全量通过（如有因 API 变更导致的失败，记录失败用例数量和错误信息，进入 Task 2 修复）

- [ ] **Step 6: Commit**

```bash
git add pyproject.toml uv.lock
git commit -m "chore: upgrade nanobot-ai from 0.1.5.post2 to 0.2.0"
```

---

## Task 2: 全量测试修复

**Files:**
- Modify: 因 API 变更失败的测试文件（具体文件取决于 Step 5 的失败结果）

- [ ] **Step 1: 运行全量测试并记录失败用例**

Run: `uv run pytest tests/ -v --tb=short 2>&1 | Select-String "FAILED|ERROR"`
Expected: 列出所有失败用例（若无失败则跳过本 Task）

- [ ] **Step 2: 逐个修复失败用例**

根据失败原因修复代码。常见修复模式：
- 如果 `AgentHook` 新增了必须实现的抽象方法：在 `DecisionLogHook` 中添加空实现
- 如果 `AgentHookContext` 属性变更：更新 mock_context fixture
- 如果 nanobot-ai 内部行为变更导致间接失败：调整测试预期

- [ ] **Step 3: 运行 ruff 检查**

Run: `uv run ruff check src/`
Expected: 无新增错误

- [ ] **Step 4: 运行 mypy 检查**

Run: `uv run mypy src/ --ignore-missing-imports`
Expected: 无新增错误

- [ ] **Step 5: 运行全量测试确认修复**

Run: `uv run pytest tests/ -x --timeout=60`
Expected: 全量通过

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "fix: resolve test failures after nanobot-ai 0.2.0 upgrade"
```

---

## Task 3: 依赖冲突预检

**Files:**
- 无代码修改，仅验证

- [ ] **Step 1: 检查 nanobot-ai 0.2.0 间接依赖**

Run: `uv pip list | Select-String "nanobot|pydantic"`
Expected: 列出 nanobot-ai 及其间接依赖版本

- [ ] **Step 2: 验证 pydantic 版本兼容性**

Run: `uv run python -c "import pydantic; print(pydantic.__version__)"`
Expected: pydantic 版本与 RunFlowAgent 现有依赖兼容（≥2.0.0）

- [ ] **Step 3: 运行 dry-run 安装检查**

Run: `uv sync --dry-run`
Expected: 无依赖冲突

- [ ] **Step 4: 记录预检结果**

若发现冲突，在 `docs/planning/task_list_v0.26.0.md` 末尾追加依赖冲突详情和解决方案。若无冲突，无需额外操作。

---

## Task 4: 配置兼容性验证

**Files:**
- 无代码修改，仅验证

- [ ] **Step 1: 验证现有 config.json 可被 0.2.0 Config loader 加载**

Run: `uv run python -c "from src.core.config.manager import ConfigManager; cm = ConfigManager(allow_default=True); print('Config loaded OK')"`
Expected: 输出 `Config loaded OK`

- [ ] **Step 2: 验证 AppConfig Schema 向后兼容**

Run: `uv run python -c "from src.core.config.schema import AppConfig; c = AppConfig.validate({'version': '0.25.0', 'data_dir': '/tmp/test'}); print('Schema OK')"`
Expected: 输出 `Schema OK`

- [ ] **Step 3: 验证新增字段有默认值**

确认 `model_presets` 字段在 AppConfig 中有默认值（将在 Task 8 中添加），现有 config.json 无需手动修改即可正常使用。

---

## Task 5: ToolRegistry 注册验证

**Files:**
- 无代码修改，仅验证

- [ ] **Step 1: 验证 Tool 基类兼容性**

Run: `uv run python -c "from nanobot.agent.tools.base import Tool; print('Tool base OK')"`
Expected: 输出 `Tool base OK`

- [ ] **Step 2: 验证所有工具正常注册**

Run: `uv run python -c "from src.agents.tools import RunnerTools; rt = RunnerTools.__new__(RunnerTools); print('RunnerTools import OK')"`
Expected: 输出 `RunnerTools import OK`

- [ ] **Step 3: 验证 evolution 工具注册**

Run: `uv run python -c "from src.agents.tools_evolution import *; print('Evolution tools import OK')"`
Expected: 输出 `Evolution tools import OK`

---

## Task 6: GoalState 适配

**Files:**
- Modify: `src/core/evolution/models.py`
- Modify: `src/core/evolution/decision_log_hook.py`
- Modify: `src/core/evolution/evolution_controller.py`
- Modify: `templates/SOUL.md`
- Create: `tests/unit/core/evolution/test_goal_state.py`
- Modify: `tests/unit/core/evolution/test_models.py`
- Modify: `tests/unit/core/evolution/test_decision_log_hook.py`

- [ ] **Step 1: 写 DecisionLog goal_state 字段的失败测试**

在 `tests/unit/core/evolution/test_models.py` 的 `TestDecisionLog` 类中追加：

```python
def test_decision_log_goal_state_default_none(self):
    """DecisionLog goal_state 字段默认为 None"""
    log = DecisionLog(
        decision_id="dec_gs01",
        timestamp=datetime(2026, 5, 24, 10, 0, 0),
        runner_state={"vdot": 45.0},
        decision_type=DecisionType.PLAN_ADJUSTMENT,
        tool_call_chain=[],
        prediction_snapshot=None,
        recommendation_text=None,
        execution_status="pending",
        recommendation_accepted=None,
        session_key="",
        goal_state="全马破4",
    )
    assert log.goal_state == "全马破4"

def test_decision_log_goal_state_none_when_omitted(self):
    """DecisionLog goal_state 省略时为 None"""
    log = DecisionLog(
        decision_id="dec_gs02",
        timestamp=datetime(2026, 5, 24, 10, 0, 0),
        runner_state={"vdot": 45.0},
        decision_type=DecisionType.TRAINING_ADVICE,
        tool_call_chain=[],
        prediction_snapshot=None,
        recommendation_text=None,
        execution_status="pending",
        recommendation_accepted=None,
        session_key="",
    )
    assert log.goal_state is None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/unit/core/evolution/test_models.py::TestDecisionLog::test_decision_log_goal_state_default_none -v`
Expected: FAIL — `DecisionLog.__init__() got an unexpected keyword argument 'goal_state'`

- [ ] **Step 3: 在 DecisionLog 数据模型中新增 goal_state 字段**

在 `src/core/evolution/models.py` 的 `DecisionLog` dataclass 中，在 `session_key` 字段后新增：

```python
    session_key: str
    goal_state: str | None = None
```

同步更新 `to_dict()` 方法，在 `session_key` 行后新增：

```python
            "goal_state": self.goal_state,
```

同步更新 `from_dict()` 方法，在 `session_key` 行后新增：

```python
            goal_state=data.get("goal_state"),
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/unit/core/evolution/test_models.py::TestDecisionLog::test_decision_log_goal_state_default_none tests/unit/core/evolution/test_models.py::TestDecisionLog::test_decision_log_goal_state_none_when_omitted -v`
Expected: PASS

- [ ] **Step 5: 写 DecisionLogHook after_iteration 读取 goal_state 的失败测试**

创建 `tests/unit/core/evolution/test_goal_state.py`：

```python
from __future__ import annotations

import asyncio
import shutil
import tempfile
from pathlib import Path
from typing import Any
from unittest.mock import MagicMock

import pytest
from nanobot.agent.hook import AgentHookContext

from src.core.evolution.decision_log_hook import DecisionLogHook
from src.core.evolution.decision_logger import DecisionLogger
from src.core.evolution.evolution_engine import EvolutionEngine
from src.core.evolution.evolution_store import EvolutionStore
from src.core.evolution.outcome_collector import OutcomeCollector


def run_async(coro: Any) -> Any:
    try:
        loop = asyncio.get_running_loop()
    except RuntimeError:
        loop = None
    if loop and loop.is_running():
        import concurrent.futures
        with concurrent.futures.ThreadPoolExecutor() as pool:
            future = pool.submit(asyncio.run, coro)
            return future.result()
    else:
        return asyncio.run(coro)


@pytest.fixture
def temp_dir() -> Path:
    dir_path = Path(tempfile.mkdtemp())
    yield dir_path
    if dir_path.exists():
        shutil.rmtree(dir_path)


@pytest.fixture
def engine(temp_dir: Path) -> EvolutionEngine:
    store = EvolutionStore(temp_dir)
    decision_logger = DecisionLogger(store)
    outcome_collector = OutcomeCollector(store, decision_logger)
    return EvolutionEngine(
        decision_logger=decision_logger,
        outcome_collector=outcome_collector,
    )


@pytest.fixture
def hook(engine: EvolutionEngine) -> DecisionLogHook:
    return DecisionLogHook(evolution_engine=engine, session_key="test_goal_state")


@pytest.fixture
def mock_context() -> MagicMock:
    ctx = MagicMock(spec=AgentHookContext)
    ctx.iteration = 1
    ctx.messages = []
    ctx.tool_calls = []
    ctx.tool_results = []
    ctx.final_content = None
    ctx.error = None
    ctx.metadata = {}
    return ctx


class TestGoalStateIntegration:
    """GoalState 集成测试"""

    def test_after_iteration_reads_goal_state_from_metadata(
        self, hook: DecisionLogHook, engine: EvolutionEngine, mock_context: MagicMock
    ) -> None:
        """after_iteration 应从 context.metadata 读取 goal_state"""
        mock_context.metadata = {"goal_state": "全马破4"}
        hook.finalize_content(mock_context, "以下是训练建议")
        hook.after_iteration(mock_context)

        decisions = engine.get_decision_history(limit=10)
        assert len(decisions) >= 1
        assert decisions[0].goal_state == "全马破4"

    def test_after_iteration_no_goal_state_in_metadata(
        self, hook: DecisionLogHook, engine: EvolutionEngine, mock_context: MagicMock
    ) -> None:
        """metadata 中无 goal_state 时 DecisionLog.goal_state 应为 None"""
        mock_context.metadata = {}
        hook.finalize_content(mock_context, "以下是训练建议")
        hook.after_iteration(mock_context)

        decisions = engine.get_decision_history(limit=10)
        assert len(decisions) >= 1
        assert decisions[0].goal_state is None

    def test_goal_state_raw_helper(
        self, hook: DecisionLogHook
    ) -> None:
        """goal_state_raw 辅助方法应正确提取 metadata 中的 goal_state"""
        metadata = {"goal_state": "半马PB130"}
        result = hook.goal_state_raw(metadata)
        assert result == "半马PB130"

    def test_goal_state_raw_empty_metadata(
        self, hook: DecisionLogHook
    ) -> None:
        """goal_state_raw 在 metadata 为空时返回 None"""
        result = hook.goal_state_raw({})
        assert result is None

    def test_goal_state_raw_none_metadata(
        self, hook: DecisionLogHook
    ) -> None:
        """goal_state_raw 在 metadata 为 None 时返回 None"""
        result = hook.goal_state_raw(None)
        assert result is None
```

- [ ] **Step 6: 运行测试确认失败**

Run: `uv run pytest tests/unit/core/evolution/test_goal_state.py -v`
Expected: FAIL — `DecisionLogHook` 没有 `goal_state_raw` 方法

- [ ] **Step 7: 在 DecisionLogHook 中新增 goal_state_raw 方法和 after_iteration 中的 GoalState 读取**

在 `src/core/evolution/decision_log_hook.py` 中：

1. 在 `__init__` 方法中，在 `self._decision_logged: bool = False` 后新增：
```python
        self._current_goal_state: str | None = None
```

2. 在 `before_iteration` 方法中，在 `self._decision_logged = False` 后新增：
```python
        self._current_goal_state = None
```

3. 新增 `goal_state_raw` 静态方法（在 `_infer_decision_type` 方法前）：
```python
    @staticmethod
    def goal_state_raw(metadata: dict[str, Any] | None) -> str | None:
        """从 context.metadata 提取当前活跃目标

        Args:
            metadata: AgentHookContext.metadata 字典

        Returns:
            str | None: 目标字符串，无目标时返回 None
        """
        if metadata is None:
            return None
        return metadata.get("goal_state")
```

4. 修改 `after_iteration` 方法，在 `if not self._decision_logged:` 检查之前，新增 GoalState 读取逻辑：
```python
    def after_iteration(self, context: Any) -> None:
        """Agent迭代完成后回调（v0.26扩展：读取GoalState + 触发进化检查）"""
        # v0.26: 读取 GoalState
        metadata = getattr(context, "metadata", None)
        goal_state = self.goal_state_raw(metadata)
        if goal_state is not None:
            self._current_goal_state = goal_state

        if not self._decision_logged:
            return

        # v0.25: 触发进化检查
```

5. 修改 `finalize_content` 方法中创建 DecisionLog 的部分，在 `session_key=self._session_key,` 后新增：
```python
            goal_state=self._current_goal_state,
```

- [ ] **Step 8: 运行测试确认通过**

Run: `uv run pytest tests/unit/core/evolution/test_goal_state.py -v`
Expected: 全部 PASS

- [ ] **Step 9: 在 SOUL.md 新增 GoalState 使用指导段落**

在 `templates/SOUL.md` 末尾追加：

```markdown

## GoalState 使用指导

当用户创建训练计划或设定跑步目标时，你**必须**使用 `long_task` Skill 将目标持久化到 GoalState，确保跨对话可回忆。

### 使用方式
1. 用户提出目标（如"我要全马破4"）→ 调用 `long_task` Skill 创建持久化目标
2. GoalState 会自动写入 `session.metadata`，后续对话中可自动读取
3. 在新对话中，优先检查当前 GoalState，确认用户是否有进行中的目标

### 示例
- 用户："帮我制定一个全马破4的训练计划" → 创建 GoalState: "全马破4"
- 用户："我现在的训练目标是什么？" → 读取 GoalState 回答
- 目标完成后 → 更新 GoalState 状态为 completed
```

- [ ] **Step 10: 在 EvolutionController 月度复盘中新增 GoalState 检查**

在 `src/core/evolution/evolution_controller.py` 的 `_check_monthly_review_trigger` 方法中，在 `trigger_condition` 字典中新增 `goal_state_active` 字段：

将：
```python
            trigger_condition={
                "current_month": current_month,
                "last_report_month": last_report_month,
            },
```

修改为：
```python
            trigger_condition={
                "current_month": current_month,
                "last_report_month": last_report_month,
                "goal_state_active": True,
            },
```

- [ ] **Step 11: 运行全量测试确认无回归**

Run: `uv run pytest tests/unit/core/evolution/ -v --timeout=60`
Expected: 全部 PASS

- [ ] **Step 12: Commit**

```bash
git add src/core/evolution/models.py src/core/evolution/decision_log_hook.py src/core/evolution/evolution_controller.py templates/SOUL.md tests/unit/core/evolution/test_goal_state.py tests/unit/core/evolution/test_models.py
git commit -m "feat: adapt GoalState from nanobot-ai 0.2.0 (ADR-012)"
```

---

## Task 7: 推理可见化适配

**Files:**
- Modify: `src/core/evolution/decision_log_hook.py`
- Create: `tests/unit/core/evolution/test_reasoning.py`
- Modify: `tests/unit/core/evolution/test_decision_log_hook.py`

- [ ] **Step 1: 写 emit_reasoning 方法的失败测试**

创建 `tests/unit/core/evolution/test_reasoning.py`：

```python
from __future__ import annotations

import asyncio
import shutil
import tempfile
from pathlib import Path
from typing import Any
from unittest.mock import MagicMock

import pytest
from nanobot.agent.hook import AgentHookContext

from src.core.evolution.decision_log_hook import DecisionLogHook
from src.core.evolution.decision_logger import DecisionLogger
from src.core.evolution.evolution_engine import EvolutionEngine
from src.core.evolution.evolution_store import EvolutionStore
from src.core.evolution.outcome_collector import OutcomeCollector


def run_async(coro: Any) -> Any:
    try:
        loop = asyncio.get_running_loop()
    except RuntimeError:
        loop = None
    if loop and loop.is_running():
        import concurrent.futures
        with concurrent.futures.ThreadPoolExecutor() as pool:
            future = pool.submit(asyncio.run, coro)
            return future.result()
    else:
        return asyncio.run(coro)


@pytest.fixture
def temp_dir() -> Path:
    dir_path = Path(tempfile.mkdtemp())
    yield dir_path
    if dir_path.exists():
        shutil.rmtree(dir_path)


@pytest.fixture
def engine(temp_dir: Path) -> EvolutionEngine:
    store = EvolutionStore(temp_dir)
    decision_logger = DecisionLogger(store)
    outcome_collector = OutcomeCollector(store, decision_logger)
    return EvolutionEngine(
        decision_logger=decision_logger,
        outcome_collector=outcome_collector,
    )


@pytest.fixture
def hook(engine: EvolutionEngine) -> DecisionLogHook:
    return DecisionLogHook(evolution_engine=engine, session_key="test_reasoning")


@pytest.fixture
def mock_context() -> MagicMock:
    ctx = MagicMock(spec=AgentHookContext)
    ctx.iteration = 1
    ctx.messages = []
    ctx.tool_calls = []
    ctx.tool_results = []
    ctx.final_content = None
    ctx.error = None
    ctx.metadata = {}
    return ctx


class TestReasoningVisualization:
    """推理可见化测试"""

    def test_emit_reasoning_appends_to_buffer(
        self, hook: DecisionLogHook, mock_context: MagicMock
    ) -> None:
        """emit_reasoning 应将推理片段追加到缓冲区"""
        hook.emit_reasoning(mock_context, "分析用户VDOT趋势...")
        hook.emit_reasoning(mock_context, "VDOT呈上升趋势，建议增加训练量")
        assert len(hook._reasoning_buffer) == 2
        assert hook._reasoning_buffer[0] == "分析用户VDOT趋势..."
        assert hook._reasoning_buffer[1] == "VDOT呈上升趋势，建议增加训练量"

    def test_emit_reasoning_end_marks_completion(
        self, hook: DecisionLogHook, mock_context: MagicMock
    ) -> None:
        """emit_reasoning_end 应标记推理结束"""
        hook.emit_reasoning(mock_context, "推理片段")
        hook.emit_reasoning_end(mock_context)
        assert hook._reasoning_complete is True

    def test_finalize_content_includes_reasoning_snapshot(
        self, hook: DecisionLogHook, engine: EvolutionEngine, mock_context: MagicMock
    ) -> None:
        """finalize_content 应将推理缓冲区写入 DecisionLog 上下文快照"""
        hook.emit_reasoning(mock_context, "第一步推理")
        hook.emit_reasoning(mock_context, "第二步推理")
        hook.emit_reasoning_end(mock_context)

        hook.finalize_content(mock_context, "以下是训练建议")

        decisions = engine.get_decision_history(limit=10)
        assert len(decisions) >= 1
        snapshot = decisions[0].prediction_snapshot
        assert snapshot is not None
        assert "reasoning" in snapshot
        assert snapshot["reasoning"] == "第一步推理\n第二步推理"

    def test_finalize_content_without_reasoning(
        self, hook: DecisionLogHook, engine: EvolutionEngine, mock_context: MagicMock
    ) -> None:
        """无推理时 prediction_snapshot 保持 None"""
        hook.finalize_content(mock_context, "以下是训练建议")

        decisions = engine.get_decision_history(limit=10)
        assert len(decisions) >= 1
        assert decisions[0].prediction_snapshot is None

    def test_reasoning_buffer_cleared_after_finalize(
        self, hook: DecisionLogHook, engine: EvolutionEngine, mock_context: MagicMock
    ) -> None:
        """finalize_content 后推理缓冲区应被清空"""
        hook.emit_reasoning(mock_context, "推理内容")
        hook.emit_reasoning_end(mock_context)
        hook.finalize_content(mock_context, "训练建议")

        assert hook._reasoning_buffer == []
        assert hook._reasoning_complete is False

    def test_before_iteration_resets_reasoning_state(
        self, hook: DecisionLogHook, mock_context: MagicMock
    ) -> None:
        """before_iteration 应重置推理状态"""
        hook.emit_reasoning(mock_context, "推理")
        hook.emit_reasoning_end(mock_context)

        run_async(hook.before_iteration(mock_context))

        assert hook._reasoning_buffer == []
        assert hook._reasoning_complete is False
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/unit/core/evolution/test_reasoning.py -v`
Expected: FAIL — `DecisionLogHook` 没有 `emit_reasoning` 方法

- [ ] **Step 3: 在 DecisionLogHook 中实现推理可见化**

在 `src/core/evolution/decision_log_hook.py` 中：

1. 在 `__init__` 方法中，在 `self._current_goal_state: str | None = None` 后新增：
```python
        self._reasoning_buffer: list[str] = []
        self._reasoning_complete: bool = False
```

2. 在 `before_iteration` 方法中，在 `self._current_goal_state = None` 后新增：
```python
        self._reasoning_buffer = []
        self._reasoning_complete = False
```

3. 新增 `emit_reasoning` 方法（在 `goal_state_raw` 方法后）：
```python
    def emit_reasoning(
        self, context: AgentHookContext, reasoning_text: str
    ) -> None:
        """推理片段回调（v0.26：推理可见化适配）

        将 Agent 推理片段追加到内部缓冲区，在 finalize_content 时
        写入 DecisionLog 的 prediction_snapshot。

        Args:
            context: Hook上下文
            reasoning_text: 推理片段文本
        """
        if reasoning_text:
            self._reasoning_buffer.append(reasoning_text)
```

4. 新增 `emit_reasoning_end` 方法（在 `emit_reasoning` 方法后）：
```python
    def emit_reasoning_end(self, context: AgentHookContext) -> None:
        """推理结束回调（v0.26：推理可见化适配）

        标记推理过程结束。

        Args:
            context: Hook上下文
        """
        self._reasoning_complete = True
```

5. 修改 `finalize_content` 方法中创建 DecisionLog 的部分，将 `prediction_snapshot=None` 修改为：
```python
            prediction_snapshot=self._build_reasoning_snapshot(),
```

6. 新增 `_build_reasoning_snapshot` 方法（在 `_infer_decision_type` 方法前）：
```python
    def _build_reasoning_snapshot(self) -> dict[str, Any] | None:
        """构建推理快照

        将推理缓冲区内容组装为 prediction_snapshot 字典。
        finalize 后清空缓冲区。

        Returns:
            dict[str, Any] | None: 推理快照字典，无推理内容时返回 None
        """
        if not self._reasoning_buffer:
            return None

        snapshot: dict[str, Any] = {
            "reasoning": "\n".join(self._reasoning_buffer),
        }

        self._reasoning_buffer = []
        self._reasoning_complete = False

        return snapshot
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/unit/core/evolution/test_reasoning.py -v`
Expected: 全部 PASS

- [ ] **Step 5: 运行全量 evolution 测试确认无回归**

Run: `uv run pytest tests/unit/core/evolution/ -v --timeout=60`
Expected: 全部 PASS

- [ ] **Step 6: Commit**

```bash
git add src/core/evolution/decision_log_hook.py tests/unit/core/evolution/test_reasoning.py
git commit -m "feat: adapt reasoning visualization from nanobot-ai 0.2.0 (ADR-013)"
```

---

## Task 8: Model Presets 适配

**Files:**
- Modify: `src/core/config/schema.py`
- Create: `src/cli/commands/model.py`
- Create: `src/cli/handlers/model_handler.py`
- Modify: `src/cli/commands/__init__.py`
- Modify: `src/cli/app.py`
- Create: `tests/unit/cli/test_model_cli.py`

- [ ] **Step 1: 写 model list 命令的失败测试**

创建 `tests/unit/cli/test_model_cli.py`：

```python
from __future__ import annotations

from unittest.mock import MagicMock, patch

import pytest
from typer.testing import CliRunner

from src.cli.app import app


runner = CliRunner()


class TestModelListCommand:
    """Model Presets CLI 测试"""

    @patch("src.cli.handlers.model_handler.ModelHandler.__init__", return_value=None)
    def test_model_list_shows_presets(self, mock_init: MagicMock) -> None:
        """model list 应显示预设列表"""
        with patch(
            "src.cli.handlers.model_handler.ModelHandler.list_presets",
            return_value=[
                {"name": "fast", "provider": "openai", "model": "gpt-4o-mini"},
                {"name": "quality", "provider": "anthropic", "model": "claude-sonnet-4-20250514"},
            ],
        ):
            result = runner.invoke(app, ["model", "list"])
            assert result.exit_code == 0
            assert "fast" in result.output
            assert "quality" in result.output

    @patch("src.cli.handlers.model_handler.ModelHandler.__init__", return_value=None)
    def test_model_list_no_presets(self, mock_init: MagicMock) -> None:
        """无预设时显示提示信息"""
        with patch(
            "src.cli.handlers.model_handler.ModelHandler.list_presets",
            return_value=[],
        ):
            result = runner.invoke(app, ["model", "list"])
            assert result.exit_code == 0
            assert "暂无" in result.output or "无预设" in result.output or "No" in result.output
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/unit/cli/test_model_cli.py -v`
Expected: FAIL — 找不到 `src.cli.handlers.model_handler` 模块

- [ ] **Step 3: 在 AppConfig Schema 中新增 model_presets 字段**

在 `src/core/config/schema.py` 的 `AppConfig` dataclass 中：

1. 在 `tools: dict[str, Any] | None = None` 后新增：
```python
    model_presets: dict[str, dict[str, Any]] | None = None
```

2. 在 `FIELD_TYPES` ClassVar 中新增：
```python
        "model_presets": (dict, type(None)),
```

3. 在 `to_dict()` 方法中新增：
```python
            "model_presets": self.model_presets,
```

- [ ] **Step 4: 创建 ModelHandler 业务逻辑层**

创建 `src/cli/handlers/model_handler.py`：

```python
from __future__ import annotations

from typing import Any

from src.core.base.context import AppContext, AppContextFactory


class ModelHandler:
    """Model Presets 业务逻辑层

    封装配置读取，为 CLI 命令提供 Model Presets 查询接口。
    """

    def __init__(self, context: AppContext | None = None) -> None:
        if context is None:
            context = AppContextFactory.create()
        self.context = context

    def list_presets(self) -> list[dict[str, Any]]:
        """获取 Model Presets 列表

        Returns:
            list[dict]: 预设列表，每个预设包含 name/provider/model 等字段
        """
        config = self.context.config
        presets = config.get_config().get("model_presets", {})
        if presets is None:
            return []

        result: list[dict[str, Any]] = []
        for name, preset in presets.items():
            result.append(
                {
                    "name": name,
                    "provider": preset.get("provider", ""),
                    "model": preset.get("model", ""),
                    "temperature": preset.get("temperature"),
                }
            )
        return result
```

- [ ] **Step 5: 创建 model CLI 命令模块**

创建 `src/cli/commands/model.py`：

```python
from __future__ import annotations

import typer
from rich.panel import Panel
from rich.table import Table

from src.cli.common import CLIError, console, print_error
from src.cli.handlers.model_handler import ModelHandler
from src.core.base.exceptions import NanobotRunnerError

app = typer.Typer(help="Model Presets 管理", no_args_is_help=True)


@app.command(name="list")
def list_presets() -> None:
    """查看 Model Presets 列表

    展示当前配置的所有模型预设，包括名称、Provider、模型等信息。

    Examples:
        nanobotrun model list
    """
    try:
        handler = ModelHandler()
        presets = handler.list_presets()

        if not presets:
            console.print("[yellow]暂无 Model Presets 配置[/yellow]")
            console.print(
                "[dim]在 config.json 中添加 model_presets 字段以配置预设[/dim]"
            )
            return

        table = Table(show_header=True, header_style="bold cyan")
        table.add_column("预设名称", width=15)
        table.add_column("Provider", width=15)
        table.add_column("模型", width=30)
        table.add_column("Temperature", width=12)

        for preset in presets:
            temp = preset.get("temperature")
            temp_str = f"{temp}" if temp is not None else "-"
            table.add_row(
                preset["name"],
                preset["provider"],
                preset["model"],
                temp_str,
            )

        console.print(table)
        console.print(f"\n共 [bold]{len(presets)}[/bold] 个预设")
        console.print(
            "[dim]切换预设: 在飞书/WebUI 中使用 /model <preset_name> 命令[/dim]"
        )

    except NanobotRunnerError as e:
        print_error(CLIError.storage_error(str(e)))
        raise typer.Exit(1)
```

- [ ] **Step 6: 注册 model 命令组**

在 `src/cli/commands/__init__.py` 中新增：

1. 在 import 区域新增：
```python
from src.cli.commands.model import app as model_app
```

2. 在 `__all__` 列表中新增：
```python
    "model_app",
```

在 `src/cli/app.py` 中新增：

1. 在 import 区域新增：
```python
from src.cli.commands import (
    agent_app,
    analysis_app,
    cron_app,
    data_app,
    evolution_app,
    export_app,
    gateway_app,
    model_app,
    plan_app,
    prediction_app,
    preference_app,
    report_app,
    skill_app,
    status_app,
    system_app,
    tools_app,
    transparency_app,
    twin_app,
    viz_app,
)
```

2. 在 `app.add_typer` 区域新增（在 `evolution_app` 行后）：
```python
app.add_typer(model_app, name="model")
```

- [ ] **Step 7: 运行测试确认通过**

Run: `uv run pytest tests/unit/cli/test_model_cli.py -v`
Expected: 全部 PASS

- [ ] **Step 8: 手动验证 CLI 命令**

Run: `uv run nanobotrun model list`
Expected: 显示"暂无 Model Presets 配置"或预设列表

- [ ] **Step 9: 运行全量测试确认无回归**

Run: `uv run pytest tests/ -x --timeout=60`
Expected: 全部 PASS

- [ ] **Step 10: Commit**

```bash
git add src/core/config/schema.py src/cli/commands/model.py src/cli/handlers/model_handler.py src/cli/commands/__init__.py src/cli/app.py tests/unit/cli/test_model_cli.py
git commit -m "feat: adapt Model Presets from nanobot-ai 0.2.0 (ADR-014)"
```

---

## Task 9: 性能回归检查

**Files:**
- 无代码修改，仅验证和记录

- [ ] **Step 1: 运行全量测试并记录耗时**

Run: `uv run pytest tests/ --durations=20 --timeout=60`
Expected: 全部通过，记录最慢的 20 个测试用例耗时

- [ ] **Step 2: 运行 ruff 检查**

Run: `uv run ruff check src/`
Expected: 无新增错误

- [ ] **Step 3: 运行 mypy 检查**

Run: `uv run mypy src/ --ignore-missing-imports`
Expected: 无新增错误

- [ ] **Step 4: 验证核心 CLI 命令可用**

依次运行：
```bash
uv run nanobotrun --help
uv run nanobotrun evolution status
uv run nanobotrun model list
```
Expected: 各命令正常执行，无报错

- [ ] **Step 5: 记录性能基准数据**

将测试耗时、CLI 命令响应时间记录到 `docs/planning/task_list_v0.26.0.md` 末尾的性能基准章节。

---

## Task 10: 集成验证与交付

**Files:**
- 无新增代码修改，仅全量验证

- [ ] **Step 1: 运行全量测试套件**

Run: `uv run pytest tests/ -v --timeout=60`
Expected: 全部 PASS

- [ ] **Step 2: 运行代码质量检查**

Run: `uv run ruff check src/ && uv run ruff format --check src/ && uv run mypy src/ --ignore-missing-imports`
Expected: 全部通过

- [ ] **Step 3: 验证 GoalState 功能**

确认：
- DecisionLog 数据模型包含 `goal_state` 字段
- DecisionLogHook 包含 `goal_state_raw()` 方法
- SOUL.md 包含 GoalState 使用指导段落

- [ ] **Step 4: 验证推理可见化功能**

确认：
- DecisionLogHook 包含 `emit_reasoning()` 和 `emit_reasoning_end()` 方法
- DecisionLogHook 包含 `_reasoning_buffer` 和 `_reasoning_complete` 属性
- `_build_reasoning_snapshot()` 方法正确组装推理快照

- [ ] **Step 5: 验证 Model Presets 功能**

确认：
- AppConfig Schema 包含 `model_presets` 字段
- `nanobotrun model list` 命令可用
- ModelHandler 业务逻辑层正确读取配置

- [ ] **Step 6: 验证 pyproject.toml 版本**

Run: `uv run python -c "import nanobot; print(nanobot.__version__)"`
Expected: 输出 ≥ 0.2.0

- [ ] **Step 7: Final commit**

```bash
git add -A
git commit -m "chore: v0.26.0 base upgrade and new feature adaptation complete"
```
