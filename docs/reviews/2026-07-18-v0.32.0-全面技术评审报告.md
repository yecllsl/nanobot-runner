# RunFlowAgent v0.32.0 全面技术评审报告

> **评审日期**: 2026-07-18
> **评审基线**: v0.32.0
> **评审类型**: 系统全面评审（架构一致性 + 代码质量 + 性能健壮性 + 安全性）
> **评审依据**: 架构设计说明书 v26.0.0 / AGENTS.md v12.0.0 / quality-rules.md / security-rules.md / compliance-rules.md / process-rules.md

---

## 一、评审总览

| 维度 | 评级 | 得分 | 状态 |
|------|------|------|------|
| 架构一致性 | **基本符合** | 78/100 | ⚠️ 警告 |
| 代码质量 | **需改进** | 70/100 | ⚠️ 警告 |
| 性能与健壮性 | **良好** | 82/100 | ✅ 通过 |
| 安全性 | **基本安全** | 78/100 | ⚠️ 警告 |
| **综合** | **需改进** | **80/100** | **⚠️ 警告** |

> **修正记录**（2026-07-18）：原表中"代码质量 65/100"和"综合 76/100"基于 Q-03 误报（声称核心模块零测试覆盖）。经 pytest-cov 实测验证，TOTAL 覆盖率 83% 符合基线，Q-03 已撤销。代码质量修正为 70/100，综合修正为 80/100。

---

## 二、架构一致性评审

### 2.1 模块划分与组件边界 — 基本符合

**符合项**：
- 14 个子模块与架构设计说明书 §4 完全一致，无遗漏无冗余
- 依赖注入模式（`AppContext` + `AppContextFactory`）正确实施，CLI 和 Agent 均通过 `get_context()` 获取依赖
- `ImportService`、`PlanManager` 等接收 `AppContext` 而非直接实例化子组件，符合松耦合设计

**偏离项**：

| 编号 | 严重度 | 问题 | 位置 |
|------|--------|------|------|
| M-01 | 中 | AGENTS.md 和 development_guide.md 中 import 路径有误：`from src.core.context import get_context` 应为 `from src.core.base.context import get_context` | [AGENTS.md](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/AGENTS.md) 多处；[development_guide.md:L273](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/docs/guides/development_guide.md#L273) |
| M-02 | 低 | `StorageManager.__init__()` 在 fallback 路径中直接实例化 `ConfigManager()`，属 Bootstrap 必要例外但缺注释 | [parquet_manager.py:L30-L32](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/storage/parquet_manager.py#L30-L32) |

### 2.2 ADR 决策审查 — 基本符合

**11/12 项 ADR 符合设计**：

| ADR | 决策 | 验证 |
|-----|------|------|
| ADR-007 | DecisionLogHook 独立继承 AgentHook | ✅ [decision_log_hook.py:L45](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/evolution/decision_log_hook.py#L45) |
| ADR-008 | 线性修正 + EMA α=0.7 ±10% | ✅ [calibration_engine.py:L26-L80](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/evolution/calibration_engine.py#L26-L80) |
| ADR-009 | 规则引擎 4 触发 + 异步 daemon 线程 | ✅ [evolution_controller.py:L130-L173](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/evolution/evolution_controller.py#L130-L173) + [decision_log_hook.py:L217-L227](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/evolution/decision_log_hook.py#L217-L227) |
| ADR-010 | 4 维提示调优参数空间 | ✅ [prompt_tuner.py:L18-L53](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/evolution/prompt_tuner.py#L18-L53) |
| ADR-015~017 | WebSocket 通道配置/启用/认证 | ✅ config.example.json + gateway.py |
| ADR-018~022 | 独立 Web 服务/数据一致性/并发安全 | ✅ [app.py](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/webui/app.py) + `run_in_threadpool` |
| ADR-027 | SDKAdapter 编程式调用入口 | ✅ [sdk_adapter.py](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/sdk_adapter.py) |

**1 项 ADR 偏离**：

| 编号 | 严重度 | 问题 | 影响 |
|------|--------|------|------|
| **D-01** | **🔴 高** | **ConfigInjector 缺失（ADR-026 未实现）**：`src/core/config_injector.py` 不存在，配置注入层未实现。nanobot Config 构建逻辑分散在 RunnerProviderAdapter 和测试代码中 | 架构核心承诺未兑现，配置转换层缺失 |

### 2.3 数据流架构 — 符合

- FIT → FitParser → IndexManager(SHA256) → StorageManager(Parquet 按年分片) 完整链路验证通过 ✅
- `AppContextFactory.create()` → `AppContext` → `get_context()` → CLI/Agent 依赖注入链路完整 ✅
- LazyFrame 优先策略在 SessionRepository 中正确实施 ✅

### 2.4 配置文件结构 — 基本符合

| 编号 | 严重度 | 问题 | 位置 |
|------|--------|------|------|
| C-01 | 中 | `_setup_fastapi_server()` 使用内联硬编码配置而非 `get_webui_config()` | [gateway.py:L399-L402](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/cli/commands/gateway.py#L399-L402) |
| C-02 | 低 | `config.example.json` 未更新以反映 v0.32.0 配置分离架构 | [config.example.json:L50-L73](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/config.example.json#L50-L73) |

---

## 三、代码质量评审

### 3.1 静态分析结果

| 检查项 | 结果 |
|--------|------|
| ruff check | ✅ 零警告 |
| mypy type check | ✅ 零错误 |
| 单元测试 | ✅ 4378 passed, 1 skipped, 137 warnings (93.27s) |
| 总覆盖率 | 83%（source: pytest-cov） |

### 3.2 代码规范遵循度 — 需改进

**P0 级（阻塞发布）**：

| 编号 | 问题 | 位置 |
|------|------|------|
| Q-01 | 空 except 块吞没错误（`except OSError: pass`） | [backup_manager.py:L381](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/config/backup_manager.py#L381) |
| Q-02 | 裸 Exception 无日志 | [profile.py:L344](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/base/profile.py#L344) |

> **修正记录**（2026-07-18）：原 Q-03 项"核心模块零测试覆盖"为误报。经 pytest-cov 实测验证，所有声称"零覆盖"的核心模块实际覆盖率 82-100%，TOTAL 83% 已超过基线要求（core ≥ 80%）。详见 §3.1 与 §七。误报根源：子 Agent 基于"测试文件存在性"判断而非读取实测覆盖率报告。

**P1 级（重要）**：

| 编号 | 问题 | 影响范围 |
|------|------|----------|
| Q-04 | `dict[str, Any]` 广泛使用 | 全项目（tools.py 系列、evolution/models.py、trace_logger.py 等） |
| Q-05 | LazyFrame 过早 collect() | [anomaly_filter.py:L107](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/report/anomaly_filter.py#L107) 两次 collect + [profile.py:L342](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/base/profile.py#L342) |
| Q-06 | 裸 Exception 13+ 处 | [tools.py](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/agents/tools.py)、[parser.py](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/storage/parser.py)、[parquet_manager.py](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/storage/parquet_manager.py) 等 |
| Q-07 | 巨型文件 | [tools.py](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/agents/tools.py)(3179 行)、[analytics.py](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/analytics.py)(1314 行)、[report/service.py](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/report/service.py)(1196 行) |

### 3.3 可读性与注释 — 良好

- 命名规范：PascalCase 类名 / snake_case 函数 / UPPER_SNAKE_CASE 常量 全部合规 ✅
- 公共 API 均有 Google 风格 docstring ✅
- 无遗留 TODO/FIXME/HACK 标记 ✅
- 重复代码：`anomaly_filter` 逻辑在 [profile.py](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/base/profile.py) 和 [anomaly_filter.py](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/report/anomaly_filter.py) 中重复；`_is_empty_lazyframe` 在 profile.py 和 session_repository.py 中重复

### 3.4 错误处理 — 需改进

- ✅ 自定义异常体系完善：`NanobotRunnerError` → 7 个子类，每个携带 `error_code` + `recovery_suggestion`
- ✅ 错误分类器支持二级分类和友好提示
- ✅ 三层降级策略（ML → 参数化 → 基础），数据不足自动降级
- ⚠️ 异常处理模式不一致：`tools.py` 中同时存在 `except NanobotRunnerError` 和 `except Exception` 两种模式

---

## 四、性能与健壮性评审

### 4.1 缓存策略 — 良好

| 缓存类型 | 实现 | 位置 |
|----------|------|------|
| 同日缓存 | PredictionEngine 同日缓存（VDOT/Race/Injury） | [prediction_engine.py:L49-L73](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/prediction/prediction_engine.py#L49-L73) |
| TTL 缓存 | StateVectorCache TTL=24h | [twin/models.py:L292-L315](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/twin/models.py#L292-L315) |
| 配置缓存 | ConfigManager 缓存 TTL=300s | [manager.py:L44](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/config/manager.py#L44) |
| 特征缓存 | FeatureEngine 特征矩阵缓存 | [feature_engine.py:L288](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/prediction/feature_engine.py#L288) |

### 4.2 并发与异步 — 良好

| 模式 | 实现 | 位置 |
|------|------|------|
| 线程池隔离 | WebUI API 使用 `run_in_threadpool` 包装同步核心方法 | [evolution.py:L101](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/webui/routes/evolution.py#L101) 等 |
| 异步进化 | `threading.Thread(daemon=True)` 不阻塞主流程 | [decision_log_hook.py:L217-L227](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/evolution/decision_log_hook.py#L217-L227) |
| SSE 流式推送 | asyncio.Queue + 心跳保活 | [runtime_events.py:L33-L68](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/webui/routes/runtime_events.py#L33-L68) |
| 向量化计算 | EWMA 向量化计算性能提升 50%+ | [training_load_analyzer.py:L263-L285](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/calculators/training_load_analyzer.py#L263-L285) |

### 4.3 资源管理 — 良好

- `AppContext` 使用 `reset_context()` 支持测试隔离 ✅
- 磁盘缓存写入使用 try/except 保护，失败不影响主流程 ✅
- 导出引擎使用 `BATCH_SIZE=100` 分批处理避免内存占用过大 ✅

---

## 五、安全性评审

### 5.1 各维度评级

| 维度 | 评级 | 关键发现 |
|------|------|----------|
| 输入验证 | **有风险** | FIT 文件缺少大小限制、CLI 路径未规范化 |
| 敏感数据保护 | **基本安全** | 无硬编码密钥，.gitignore 基本完善 |
| 访问控制 | **基本安全** | WebUI 全部路由有 JWT 认证，SPA fallback 存在路径穿越 |
| 合规检查 | **基本安全** | bandit/safety 已配置，B601 跳过需关注 |
| 常见安全漏洞 | **有风险** | joblib 反序列化风险、SPA 路径穿越 |

### 5.2 P1 级安全风险

| 编号 | 风险 | 位置 | 修复工作量 |
|------|------|------|-----------|
| **S-01** | SPA fallback 路径穿越（可读取任意文件） | [app.py:L192-L199](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/webui/app.py#L192-L199) | 15 分钟 |
| **S-02** | FIT 文件解析无大小限制（DoS 风险） | [parser.py:L117-L181](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/storage/parser.py#L117-L181) | 15 分钟 |
| **S-03** | CLI 数据导入路径未规范化 | [data.py:L27-L31](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/cli/commands/data.py#L27-L31) | 10 分钟 |

### 5.3 P2 级安全风险

| 编号 | 风险 | 位置 |
|------|------|------|
| S-04 | joblib.load() 反序列化风险（pickle） | [logistic_injury.py:L60-L68](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/src/core/prediction/baselines/logistic_injury.py#L60-L68) |
| S-05 | bandit 配置跳过 B601（subprocess 命令注入检测） | [pyproject.toml:L151](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/pyproject.toml#L151) |
| S-06 | .gitignore 缺少 `config.json` 显式排除 | [.gitignore](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/.gitignore) |
| S-07 | 前端 token 存储在 localStorage（XSS 泄露风险） | [client.ts:L40](file:///d:/yecll/Documents/LocalCode/RunFlowAgent/webui/src/api/client.ts#L40) |

### 5.4 安全合规项（通过）

- ✅ 无硬编码密钥/Token
- ✅ 无 `eval()` 使用
- ✅ `.gitignore` 排除 `*.parquet`、`*.fit`、`nanobot_config.json`
- ✅ MD5 使用 `usedforsecurity=False` 声明
- ✅ WebUI 全部业务路由有 `Depends(get_current_user)` JWT 认证
- ✅ 无 SQL 注入（使用 Parquet 存储）
- ✅ 无 `dangerouslySetInnerHTML`（React 默认转义输出）

---

## 六、综合问题清单与优先级

### P0 — 关键（阻塞发布，需立即修复）

| 编号 | 维度 | 问题 | 修复建议 |
|------|------|------|----------|
| **D-01** | 架构 | ConfigInjector 缺失（ADR-026 未实现） | 方案 B：更新架构文档承认配置注入由 RunnerProviderAdapter 承担（用户决策 2026-07-18） |
| **Q-01** | 质量 | 空 except 块吞没错误 | 添加 `logger.warning()` 日志 |
| **Q-02** | 质量 | 裸 Exception 无日志 | 替换为具体异常 + 日志 |

> **修正记录**（2026-07-18）：原 P0 清单中的 Q-03"核心模块零测试覆盖"为误报，已删除。实测 TOTAL 覆盖率 83%，符合 core ≥ 80% 基线要求。

### P1 — 重要（下个迭代修复）

| 编号 | 维度 | 问题 | 修复建议 |
|------|------|------|----------|
| **S-01** | 安全 | SPA fallback 路径穿越 | 增加 `resolve()` 校验，确保路径在 `dist_dir` 内 |
| **S-02** | 安全 | FIT 文件无大小限制 | 增加 50MB 大小上限校验 |
| **S-03** | 安全 | CLI 导入路径未规范化 | 增加 `Path.resolve()` 和 `..` 拒绝 |
| **D-02** | 架构 | `get_websocket_config()` vs `get_webui_config()` 命名不一致 | 统一命名或添加别名 |
| **M-01** | 架构 | AGENTS.md 中 import 路径错误 | 修正为 `from src.core.base.context import get_context` |
| **Q-04** | 质量 | `dict[str, Any]` 广泛使用 | 核心模块迁移到 TypedDict/dataclass |
| **Q-05** | 质量 | LazyFrame 过早 collect() | 合并 collect 调用或消除 collect |
| **Q-06** | 质量 | 13+ 处裸 Exception | 替换为具体异常或包装为 NanobotRunnerError |
| **Q-07** | 质量 | 巨型文件拆分 | 按功能域拆分子模块 |

### P2 — 中等（技术债跟踪）

| 编号 | 维度 | 问题 | 修复建议 |
|------|------|------|----------|
| S-04 | 安全 | joblib 反序列化安全加固 | 增加模型文件完整性校验 |
| S-05 | 安全 | bandit B601 全局跳过 | 移除全局跳过，改为逐行 `# nosec` |
| S-06 | 安全 | .gitignore 缺少 `config.json` | 增加 `/config.json` 排除 |
| C-01 | 架构 | `_setup_fastapi_server()` 配置读取不一致 | 统一使用 `get_webui_config()` |
| D-03 | 架构 | shap 依赖声明 | 架构文档中标注为可选依赖 |
| Q-08 | 质量 | 异常过滤逻辑重复 | 统一使用 `AnomalyDataFilter` |
| Q-09 | 质量 | `_is_empty_lazyframe` 重复 | 抽取公共工具函数 |

### P3 — 低风险（改进建议）

| 编号 | 维度 | 问题 | 修复建议 |
|------|------|------|----------|
| M-02 | 架构 | StorageManager Bootstrap 例外缺注释 | 添加 `ponytail:` 注释说明 |
| C-02 | 架构 | config.example.json 未更新 | 反映 v0.32.0 配置分离架构 |
| S-08 | 安全 | CORS `allow_methods=["*"]` 过于宽松 | 限制为实际需要的 methods |
| S-09 | 安全 | 健康检查端点暴露版本信息 | 移除版本号或在认证后返回 |
| S-10 | 安全 | API 文档端点生产环境暴露 | 通过配置项控制文档端点 |

---

## 七、与项目基线对比分析

| 基线要求 | 当前状态 | 差距 | 趋势 |
|----------|----------|------|------|
| core 测试覆盖率 ≥ 80% | **83%（实测 TOTAL）** | 达标 | 🟢 |
| agents 覆盖率 ≥ 70% | ~60%（tools 系列有测试） | 接近 | 🟡 |
| cli 覆盖率 ≥ 60% | ~70%（有 1434 行测试） | 达标 | 🟢 |
| 禁止裸 Exception | 15+ 处违规 | 需整改 | 🔴 |
| 禁止 `dict[str, Any]` | 广泛使用 | 需整改 | 🟡 |
| 禁止 LazyFrame 过早 collect() | 2 处违规 | 需整改 | 🟡 |
| 依赖注入规范 | 正确实施 | 达标 | 🟢 |
| 安全规则（输入验证） | 3 项未达标 | 需整改 | 🔴 |
| ruff check 零警告 | ✅ | 达标 | 🟢 |
| mypy 类型检查通过 | ✅ | 达标 | 🟢 |
| 4378 单元测试全部通过 | ✅ | 达标 | 🟢 |
| 版本号一致性 | ✅ v0.32.0 | 达标 | 🟢 |

> **修正记录**（2026-07-18）：原表中"core 覆盖率 30-40%"为误报。实测数据：TOTAL 83%，analytics.py 95%、report/service.py 82%、plan/plan_manager.py 98%、transparency/error_classifier.py 100%、prediction/prediction_engine.py 100%、twin/digital_twin_engine.py 95%。所有声称"零覆盖"的核心模块实际均有 82-100% 覆盖率。

---

## 八、各维度评分汇总

| 维度 | 评级 | 得分 | 说明 |
|------|------|------|------|
| 模块划分与组件边界 | 基本符合 | 80/100 | 文档路径错误，Bootstrap 缺注释 |
| 设计模式一致性（ADR） | 基本符合 | 75/100 | ConfigInjector 缺失（ADR-026 未实现） |
| 数据流架构 | 符合 | 95/100 | 完整链路，LazyFrame 优先 |
| 配置文件结构 | 基本符合 | 80/100 | 配置读取不一致，示例文件未更新 |
| 代码规范遵循度 | 需改进 | 60/100 | 裸 Exception 15+ 处，`dict[str, Any]` 广泛使用 |
| 可读性与可维护性 | 良好 | 80/100 | 命名规范，模块组织清晰 |
| 注释完整性 | 良好 | 85/100 | 公共 API docstring 规范 |
| 测试覆盖率 | **良好** | **85/100** | **实测 TOTAL 83%，超过 80% 基线** |
| 错误处理 | 需改进 | 60/100 | 异常体系优秀，处理模式不一致 |
| 缓存策略 | 良好 | 90/100 | 多层缓存，TTL 控制 |
| 并发与异步 | 良好 | 85/100 | run_in_threadpool + daemon 线程 |
| 资源管理 | 良好 | 85/100 | 分批处理，磁盘保护 |
| 输入验证 | 有风险 | 60/100 | FIT 大小限制、CLI 路径规范化缺失 |
| 敏感数据保护 | 基本安全 | 80/100 | .gitignore 缺少 config.json |
| 访问控制 | 基本安全 | 75/100 | SPA 路径穿越 P1 |
| 合规检查 | 基本安全 | 80/100 | bandit B601 跳过 |
| **综合** | **需改进** | **80/100** | **⚠️ 警告** |

> **修正记录**（2026-07-18）：测试覆盖率评分从 20/100 修正为 85/100（实测数据替代误报估计）；代码规范遵循度从 55/100 修正为 60/100（P0 项从 3 个减为 2 个）；综合评分从 76/100 修正为 80/100。

---

## 九、建议的 v0.33.0 整改路线

### 第一阶段：P0 修复（预计 2-3 天）

1. **更新架构文档承认 ConfigInjector 职责归属**（D-01，方案 B）
   - 修正 AGENTS.md §3.1 代码库结构：删除 `src/core/config_injector.py` 行或改为说明该职责由 `RunnerProviderAdapter` 承担
   - 更新架构设计说明书 §13.2.2 ConfigInjector 表格行，补充 ADR-026 实际实现方式说明
   - 在 RunnerProviderAdapter 中明确标注其承担配置注入职责（ponytail: YAGNI，不补建独立模块）

2. **修复空 except 块和裸 Exception**（Q-01, Q-02）
   - `backup_manager.py:L381` 添加 `logger.warning()`
   - `profile.py:L344` 替换为具体异常 + 日志

> **修正记录**（2026-07-18）：原第 3 项"为核心模块补充基础单元测试框架（Q-03）"为误报，已删除。实测 TOTAL 覆盖率 83%，core 模块覆盖率 82-100%，均已超过基线要求，无需补充。

### 第二阶段：P1 修复（预计 2-3 天）

3. **修复 3 个 P1 安全漏洞**（S-01~S-03）
   - SPA fallback 路径穿越防护（15 分钟）
   - FIT 文件大小限制（15 分钟）
   - CLI 路径规范化（10 分钟）

4. **修正文档路径错误和方法命名不一致**（M-01, D-02）
   - 更新 AGENTS.md 和 development_guide.md
   - 统一 `get_webui_config()` 与 `get_websocket_config()`

5. **替换关键路径的裸 Exception**（Q-06）
   - 优先修复 `tools.py`、`parser.py`、`parquet_manager.py` 中的 13+ 处

### 第三阶段：P2 技术债（持续迭代）

6. **安全加固**
   - joblib 模型文件完整性校验
   - 移除 bandit B601 全局跳过
   - .gitignore 增加 `config.json`

7. **逐步迁移 `dict[str, Any]` 到 TypedDict**
   - 优先 `evolution/models.py` 中的 `to_dict()` 方法
   - Agent 工具层可保留（JSON schema 兼容格式）

8. **巨型文件拆分 + 重复代码消除**
   - `tools.py` 按功能域拆分
   - `analytics.py` 按分析类型拆分
   - 统一 `AnomalyDataFilter` 和 `_is_empty_lazyframe`

---

## 十、附录

### 附录 A：审查文件清单

| 文件 | 类型 |
|------|------|
| 架构设计说明书 v26.0.0 | 架构基线 |
| AGENTS.md v12.0.0 | 项目快速参考 |
| quality-rules.md | 质量规则 |
| security-rules.md | 安全规则 |
| compliance-rules.md | 合规规则 |
| process-rules.md | 流程规则 |
| project-rules.md | 项目总规则 |

### 附录 B：审查工具与方法

| 维度 | 工具/方法 |
|------|----------|
| 架构一致性 | 人工对照架构设计说明书 + ADR 逐项验证 |
| 代码规范 | ruff check + mypy + 人工 grep |
| 测试覆盖 | pytest-cov 覆盖率报告 |
| 安全检查 | 人工审查 + bandit 配置检查 |
| 性能分析 | 代码审查（缓存策略、并发模式、资源管理） |

### 附录 C：审查团队

| 角色 | 评审范围 |
|------|----------|
| 架构评审 Agent | 架构一致性、ADR 决策审查、技术栈合规性、数据流架构、配置文件结构 |
| 代码评审 Agent | 代码规范遵循度、可读性、注释完整性、测试覆盖率、错误处理 |
| 安全评审 Agent | 输入验证、敏感数据保护、访问控制、合规检查、常见安全漏洞 |

---

> **报告生成**: 2026-07-18
> **下次评审建议**: v0.34.0 或 v1.0 发布前