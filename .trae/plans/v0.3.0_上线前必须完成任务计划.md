# v0.3.0 上线前必须完成任务计划

## 任务概述

根据 `TST_v0.3.0_回归测试与质量评估报告.md` 的要求，上线前必须完成以下工作：

## 任务清单

### 任务1：修复代码格式问题（开发工程师）

**描述**: 执行代码格式化工具修复格式问题

**步骤**:
1. 执行 `uv run black src tests` 修复代码格式
2. 执行 `uv run isort src tests` 修复导入排序
3. 验证修复结果：`uv run black --check src tests` 和 `uv run isort --check-only src tests`

**预期结果**: 代码格式化检查全部通过

---

### 任务2：修复E2E测试用例（测试工程师）

**描述**: 修复 `test_daily_training_query_flow` 断言逻辑

**问题定位**:
- 文件: `tests/e2e/v0_2_0/test_agent_e2e_main.py`
- 问题: 断言期望返回包含"最近"或"记录"中文文本，实际返回JSON格式数据
- 根因: 测试用例断言逻辑与实际返回格式不匹配

**步骤**:
1. 读取 `tests/e2e/v0_2_0/test_agent_e2e_main.py` 定位失败用例
2. 分析 `query_recent_runs()` 方法的实际返回格式
3. 调整断言逻辑，验证JSON数据结构而非文本内容
4. 运行E2E测试验证修复

**预期结果**: E2E测试通过率达到100%

---

### 任务3：验证最终质量门禁（测试工程师）

**描述**: 执行最终回归测试验证所有指标达标

**步骤**:
1. 执行全量测试：`uv run pytest --cov=src --cov-report=term-missing`
2. 验证测试通过率 = 100%
3. 验证覆盖率 ≥ 85%
4. 验证代码质量检查全部通过
5. 更新质量评估报告

**预期结果**: 所有上线标准达标

---

## 执行顺序

```
任务1（开发工程师）修复代码格式
        ↓
任务2（测试工程师）修复E2E测试
        ↓
任务3（测试工程师）验证质量门禁
```

## 成功标准

| 指标 | 目标值 |
|------|--------|
| 测试通过率 | 100% |
| 代码格式检查 | 全部通过 |
| E2E测试通过率 | 100% |

## 风险提示

1. 测试环境差异：本次测试在Windows环境下执行
2. Agent模式未充分测试：建议上线后持续监控
3. 第三方依赖风险：部分依赖库版本警告
